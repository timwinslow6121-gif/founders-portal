---
phase: 02.5-postgresql-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/__init__.py
  - requirements.txt
  - migrations/alembic.ini
  - migrations/env.py
  - migrations/script.py.mako
  - migrations/versions/001_baseline.py
autonomous: true
requirements:
  - DB-INIT
must_haves:
  truths:
    - "Flask app starts without syntax errors (create_app() returns successfully)"
    - "flask db current runs without error and shows a migration head"
    - "migrations/ directory is committed to git and contains a baseline migration capturing all existing tables"
    - "psycopg2-binary is in requirements.txt"
  artifacts:
    - path: "app/__init__.py"
      provides: "Fixed create_app() — empty with block replaced with pass"
    - path: "requirements.txt"
      provides: "psycopg2-binary==2.9.9 present"
    - path: "migrations/versions/001_baseline.py"
      provides: "Alembic baseline migration capturing all current tables"
  key_links:
    - from: "app/__init__.py"
      to: "flask db commands"
      via: "Migrate(app, db) — must start without syntax error"
      pattern: "with app.app_context\\(\\):\\s+pass"
    - from: "migrations/versions/001_baseline.py"
      to: "PostgreSQL schema"
      via: "flask db upgrade applies this migration to create all tables"
      pattern: "def upgrade"
---

<objective>
Initialize Flask-Migrate (it has never been run), fix the syntax bug in app/__init__.py that prevents Flask from starting, add psycopg2-binary to requirements.txt, and generate a baseline Alembic migration that captures the full current schema.

Purpose: Without a migrations/ directory and a correct baseline migration, no subsequent wave can run `flask db upgrade` to create the PostgreSQL schema. This plan creates that foundation.
Output: migrations/ directory committed to git, app/__init__.py syntax-clean, psycopg2-binary in requirements.txt.
</objective>

<execution_context>
@/home/timothywinslowlinux/.claude/get-shit-done/workflows/execute-plan.md
@/home/timothywinslowlinux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.5-postgresql-migration/02.5-CONTEXT.md
@.planning/phases/02.5-postgresql-migration/02.5-RESEARCH.md

<interfaces>
<!-- Key code the executor needs. Extracted from codebase. -->

From app/__init__.py (current, BROKEN — lines 34-36):
```python
    with app.app_context():

    return app
```
The `with app.app_context():` block has an empty body — invalid Python syntax.
Fix: add `pass` as the body, or remove the block entirely (it contains nothing useful).

From requirements.txt (current — psycopg2-binary is absent):
```
flask==3.0.0
flask-sqlalchemy==3.1.1
flask-migrate==4.0.5
gunicorn==21.2.0
...
```
psycopg2-binary==2.9.9 must be added.

Tables that exist (all must appear in baseline migration):
users, policies, import_batches, commission_statements, pharmacies, customers,
customer_contacts, customer_notes, customer_aor_history, agent_carrier_contracts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix __init__.py syntax bug and add psycopg2-binary to requirements.txt</name>
  <files>app/__init__.py, requirements.txt</files>
  <action>
Fix the syntax error in app/__init__.py: lines 34-36 read `with app.app_context():` with an empty body followed immediately by `return app`. Replace the empty `with` block with `pass` as the body:

```python
    with app.app_context():
        pass

    return app
```

Do NOT remove the `with app.app_context():` block — leave it with `pass` so future code (model imports, etc.) has a place to go.

Add psycopg2-binary to requirements.txt. Insert after flask-migrate line:
```
psycopg2-binary==2.9.9
```

Verify the fix locally: `python -c "from app import create_app; app = create_app(); print('OK')"` must print OK with no IndentationError or SyntaxError.
  </action>
  <verify>
    <automated>cd /home/timothywinslowlinux/dev/founders-portal && python -c "from app import create_app; app = create_app(); print('create_app OK')"</automated>
  </verify>
  <done>create_app() runs without error. psycopg2-binary==2.9.9 present in requirements.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Flask-Migrate and generate baseline Alembic migration</name>
  <files>
    migrations/alembic.ini,
    migrations/env.py,
    migrations/script.py.mako,
    migrations/versions/001_baseline.py
  </files>
  <action>
Run the following commands from the project root on the local dev machine (Crostini). The local SQLite database exists at instance/founders_portal.db and contains the current schema — flask db migrate will detect it and generate a baseline.

```bash
export FLASK_APP=wsgi.py
flask db init
flask db migrate -m "baseline: capture existing schema"
flask db upgrade
```

After these commands:
1. `migrations/` directory must exist with alembic.ini, env.py, script.py.mako, and versions/
2. Exactly one file in migrations/versions/ named with a revision hash and containing "baseline: capture existing schema" in its docstring
3. `flask db current` must show a head (not "No migrations")

CRITICAL: The generated baseline migration file will be in migrations/versions/{hash}_baseline_capture_existing_schema.py. Do NOT rename it. Do NOT edit it unless Flask reports an error — let Alembic generate it from the live models.

After verifying locally, commit everything:
```bash
git add migrations/
git commit -m "feat(02.5-01): initialize flask-migrate, add baseline migration"
```

Do NOT add migrations/ to .gitignore — it must be tracked.
  </action>
  <verify>
    <automated>cd /home/timothywinslowlinux/dev/founders-portal && export FLASK_APP=wsgi.py && flask db current 2>&1</automated>
  </verify>
  <done>
    flask db current shows a revision hash (not empty or error).
    migrations/versions/ contains exactly one .py file (the baseline).
    git status shows migrations/ is tracked and committed.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `python -c "from app import create_app; create_app()"` — no error
- `flask db current` — shows revision hash, not empty
- `git log --oneline -1` — shows the baseline commit
- `grep psycopg2-binary requirements.txt` — line present
</verification>

<success_criteria>
Flask app starts without errors. Flask-Migrate is initialized with a committed baseline migration covering all existing tables. psycopg2-binary is in requirements.txt. VPS can now run `flask db upgrade` against a fresh PostgreSQL database to recreate the full schema.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-postgresql-migration/02.5-01-SUMMARY.md`
</output>
