---
plan: 02.5-03
status: complete
completed: 2026-03-26
---

# Summary: Install PostgreSQL on VPS

## What Was Built

PostgreSQL 16 installed on NixiHost VPS (23.187.248.100), `founders_portal` database and `founders_user` created, schema migrations 001 + 002 applied, pgloader installed.

## Key Files

### key-files.created
- `/etc/apt/sources.list.d/pgdg.list` — PGDG repo for PostgreSQL 16
- `/var/lib/postgresql/16/main/` — PostgreSQL data directory

### key-files.modified
- `/var/www/founders-portal/` — pulled latest code (migrations + models)

## Tasks Completed

| # | Task | Status |
|---|------|--------|
| 1 | Back up SQLite DB, install PostgreSQL 16, create DB/user, install pgloader, pull code | ✓ |
| 2 | Run `flask db upgrade f003d6f5cb50` — applies migrations 001 + 002 | ✓ |
| 3 | Verify all 13 tables exist, agency_id is nullable | ✓ |

## Verification

- PostgreSQL 16 running on port 5432 (`pg_lsclusters` confirmed)
- 13 tables created: agencies, agent_carrier_contracts, alembic_version, audit_logs, commission_statements, customer_aor_history, customer_contacts, customer_notes, customers, import_batches, pharmacies, policies, users
- `agency_id integer` (nullable) on users table — confirmed correct, NOT NULL comes after backfill (migration 003)
- pgloader 3.6.10 installed
- SQLite backup: `instance/founders_portal.db.backup` (2.1M, same as original)

## Database Credentials

- **Host:** localhost
- **Database:** founders_portal
- **User:** founders_user
- **Password:** stored in `.env` as `DATABASE_URL` (set in Wave 5)

## Deviations

- VPS had uncommitted `base.html` changes (admin/agent sidebar split) that conflicted with repo. Resolved by keeping stash structure and adding Pharmacies link to admin block.
- Migrations were not yet pushed to GitHub at time of `git pull` — pushed during this wave.
