# Founders Insurance Agency — Agent Portal

<!-- ════════════════════════════════════════════════════════════════════
     START HERE (new session) — keep this block current at every session end.
     ════════════════════════════════════════════════════════════════════ -->
## 🧭 START HERE — current state (updated 2026-07-14)

**✅ THIS SESSION (2026-07-14) — LOGIN REDESIGN + AGENCY NOTICE BOARD shipped + LIVE (migration 037); plan-summary v2 captured for a fresh brainstorm.** (1) **Login rebuilt** — retired the animated pestle logo (AJ said it looked like a penis 🤦); new Founders split-screen: left = public-safe **Agency Notice Board** (auto-computed **AEP countdown** pinned first + admin-managed `info`/`alert` notices), right = theme-aware Google SSO. New `AgencyNotice` model (mig 037, head now **037**), `/admin/notices` CRUD (`notices_bp`, admin nav link), defensive pre-auth read (a board read failure NEVER blocks the SSO button — it's on the pre-auth path). 2 seeded starter notices (portal-maintenance alert + beta-notice info). Built spec→plan→6-task subagent-driven-development→**opus whole-branch review (READY TO MERGE, 0 findings)** → deployed + live-verified (login 200 renders board + AEP + both notices, NO pestle, /admin/notices 302-gated, no errors). Suite **629**. Spec+plan `docs/superpowers/specs|plans/2026-07-14-login-redesign-agency-notice-board*`; handoff `memory/session-handoff-2026-07-14-login-notice-board.md`. ⚠ **Tim still to eyeball a real Google sign-in end-to-end** (can't automate OAuth from here). **Follow-up queued (brainstorm→spec):** agent-only **Upcoming Events + pay-dates feed on the DASHBOARD** (`AgencyEvent`; NOT public-safe → behind login, opposite of the notice board). (2) **Plan-summary v2** fully captured for a fresh unhurried brainstorm→spec (Gemini Consumer/Pro mockup + ChatGPT portal-native panels; **Network Snapshot = MANUAL agent-maintained** per-service-area provider directory w/ a "plays nice / bills OON" flag — the PPO tribal-knowledge gem, Charlotte=big-systems vs Kannapolis=named-local-practices; drug checker MUST be zippy client-side). Interactive mockups published as artifacts. Memory `plan-summary-consumer-pro-redesign.md`.

**✅ ALSO THIS SESSION (2026-07-15) — UHC RECONCILIATION, SAFE BUCKETS DONE (2285→2277 active, +96→+88; same careful method as Aetna).** Read-only diff first (`scripts/uhc_reconcile_diff.py`, matches member_id-OR-MBI both formats). Split the +96: **A** 5 termed-status flips (term_date already set) ✅; **B** 20 synthetic `uhc::0::` medigap stub policies merged into 8 real MBI customers + removed → **20 phantom customers gone** ✅; **D** full authoritative BOB re-imported via `scripts/import_bob_file.py` → 15 new active members created, **0 BOB-not-in-DB remaining** ✅. Money intact ($137,747.11 UHC). DB backup `/root/founders_pre_uhc_reconcile_20260715_141629.sql`. **HELD (the +88 remaining): 89 real-ID rows (88 commission-keyed, mostly eff 1/1/2026 & 1/1/2025)** = the CROSS-CARRIER SWITCHER PASS (`docs/UHC_held_bucketC_cross_carrier_2026-07-15.csv`) — needs Humana/Aetna/BCBS full BOBs to cross-check who moved + name+dob+ADDRESS; 2025/older = likely stale year-end terms. Handoff `memory/session-handoff-2026-07-15-uhc-reconciliation.md`.

**✅ ALSO THIS SESSION (2026-07-15) — MEDICARE UPDATES HUB Phase 1 shipped + LIVE (migration 038).** Behind-login shared board (`/updates`) — the single place agents check for carrier intel that moves commissions/sales. Any agent posts TYPED (commission/network/carrier_notice/training/important_date/general) + carrier-tagged updates, optionally linked to a Plan → hub shows **"Affects [Gold Plus] · 1,803 active members →"** (live count). Filter by type+carrier, owner-edit/admin-pin-delete, searchable plan picker. New `CarrierUpdate` model (mig 038), `updates_bp`, defensive `plan_affect`. 4-task subagent build + opus whole-branch review (READY TO MERGE, 0 findings), suite 638, deployed + live-verified. Grounding: "Humana killed Gold Plus commission → here's who it hits." **PHASE 2 = cached external RSS ("From around Medicare"): nightly fetch of admin-vetted sources (KFF.org + Tim gathering 5–10), keyword-filtered + admin-pinnable — own build when Tim has sources (needs `feedparser` + mig 039).** Spec+plan `docs/superpowers/specs|plans/2026-07-15-medicare-updates-hub*`; handoff `memory/session-handoff-2026-07-15-uhc-reconciliation.md` (+ updates-hub noted there). Also cleared a stale regression: plan list-vs-detail count mismatch was ALREADY fixed 07-13 (verified 0 mismatches across 75 plans live).

**▶ NEXT: (a) the CROSS-CARRIER SWITCHER PASS** (UHC's 89 held + Aetna's held 4 + Humana's held 28, all at once) once AJ delivers full Devoted/BCBS/Wellabe/GTL BOBs — closes the held rows across ALL carriers; **(b) plan-summary v2 brainstorm→spec;** or **(c) Medicare Updates Hub Phase 2 (RSS)** when Tim has sources. Carrier reconciliation is still THE #1 active priority (below).

---

## 🧭 (prev) START HERE — current state (updated 2026-07-13)

**▶▶ ACTIVE PRIORITY unchanged: CARRIER-BY-CARRIER BOB↔DB RECONCILIATION to 100% accurate customers/policies** (see the 2026-07-10 block below for the full methodology — it still governs). **▶ UHC FULL BOB ARRIVED 2026-07-13** (`docs/Carrier BOB DL/July 2026 period/UHC/UHC book of business.xlsx`, Jul-13, 422KB — CONFIRMED has `mbiNumber` + `memberNumber` + `segmentId` + `policyTermDate` + `termReasonCode`, so UHC is now RECONCILABLE, unlike the old lossy visible-rows file). **UHC target: 2,189 BOB active** (term-date sentinel 2300/2199=active; 2,188 unique people, 1 with 2 policies) **vs 2,285 DB active = +96 over-count** to work through. Tim chose to do UHC reconciliation in a FRESH focused session (money-adjacent, careful work — not an end-of-session rush). Method: diff by member_id-OR-MBI both formats, categorize the ~96 (dups / stale-active / cross-carrier switchers via name+dob+ADDRESS not dob-alone), dry-run → review → backup → apply. **Still NEED from AJ: Devoted (CSV w/ member_record_locator — current file is lossy), BCBS, Wellabe, GTL.**

**✅ THIS SESSION (2026-07-13) — a LOT shipped + LIVE: carriers-page redesign, plan-type data corrected, HealthSpring+Humana reconciled, preferred-language tag, AND a full plan-snapshot per-type redesign (built via subagent-driven-development). Read `memory/session-handoff-2026-07-13-carriers-page-plantype.md` + `-plan-snapshot-redesign.md`.**

**✅ PREFERRED-LANGUAGE TAG — SHIPPED+LIVE (migration 036).** `Customer.language` column, provenance-tracked + inline-editable on the profile (next to Medicaid) + a "Language" filter on the customers list (dropdown of distinct languages present). For Mike L's Spanish-only customers. Agent-set now; can be carrier-import-populated later (Devoted BOB has a language column). Migration head now **036**.

**✅ PLAN-SNAPSHOT PER-TYPE REDESIGN — BUILT + MERGED + DEPLOYED (5-task subagent build + opus whole-branch review, 617 tests, no migration — all in `details_json`).** The plan-detail page (`/carriers/<id>`) is now a **type-aware condensed-SOB reference card** — renders ONLY the benefits relevant to each plan type: **Part C** (Costs/Medical/Extras + OTC & Dental structured mini-blocks/Drugs-if-MAPD + year-driven OOP cap), **PDP** (premium/Rx-deductible/tiers/preferred-pharmacy/OOP cap), **Medigap** (static CMS coverage grid + "Age-rated (per quote)"), **DVH** (annual-max/dental-matrix/vision-hearing/waiting + age-rated), **Hospital Indemnity** (base benefits + free-text riders block). Member count is the ONLY headline stat; commission moved to a collapsed admin footer. Founders theme light+dark, no empty "—" rows. New `app/plan_sections.py` (per-type config + hardcoded `MEDIGAP_GRID` + `oop_cap_for_year`); `BENEFIT_KEYS` extended + admin-form inputs (auto-wired via the serializer loop). Spec `docs/superpowers/specs/2026-07-13-plan-snapshot-per-type-redesign.md`, plan `docs/superpowers/plans/2026-07-13-plan-snapshot-per-type-redesign.md`. Opus review caught 2 Important the per-task reviews missed (broken lowercase-vs-uppercase "View members" link → anchored to on-page member list; star rating dropped → restored) — both fixed. Grounded in real carrier docs: GTL/Wellabe (HI), Humana Extend 1250 + BCBS Dental Blue (DVH), CMS chart (Medigap). ⚠ Known deferred (Minor, noted in the spec/review): HI riders are free-text this pass (structured picker = follow-up); benefit values use the FLAT details_json not the provenance `_meta` seam (acceptable — matches the existing admin-form pattern); Part C benefit backfill from `pbp-benefits-2026/` = optional spec phase 6.

**Carriers & Plans page — redesigned + made accurate (Brian-demo-ready):**
- **Two count bugs fixed** (were showing wrong numbers): plan-detail counted by free-text `plan_name` not `plan_id` (→ 0/contradicting counts on EVERY carrier); and it capped the count at the `.limit()` (Gold Plus showed 500 vs real 1803). Both now count by `plan_id` + true unlimited `.count()`. Verified all carriers detail==list.
- **Full redesign** (was a cluttered 14-col table): at-a-glance **coverage summary chips** (All / Part C / PDP / Medigap / DVH, click-to-filter, Part C = MA+MAPD) + slim **4-col CMS-ID-first table** (CMS code on top, plan name under; Carrier; Type; Members) whole-row-clickable to drill down. Benefit specs live on the detail page. **Global Founders plan-type pill palette** promoted to base.html (MAPD=blue, MA=teal, PDP=violet, Medigap=gold, DVH=green, SNP=deep-blue; light+dark). Verified via headless screenshots.
- **Filters/search/sort** (all client-side, instant): **faceted refine pills** — Network [HMO/HMO-POS/PPO] + SNP [C-SNP/D-SNP] + Drug [MAPD/MA] (OR within a dimension, AND across — Tim confirmed this is what he wants); **live search** (CMS ID or plan name); **click Members header to sort**. Backed by a backfill of `plan_subtype`+`is_csnp`+`is_dsnp` parsed from plan names (106+12+14 filled — were mostly empty).
- **Punch-list fixes:** hide 0-member plans (reappear when they gain one); SilverScript Plus S5601-017 → `discontinued`; Medigap/DVH (no CMS ID) show the plan name as the top line; medigap terminology standardized to "Medigap" everywhere (labels + display names "Medigap Plan X", no carrier prefix — it's a column); reclassified 5 mislabeled "legacy" active plans → current.

**⭐ PLAN-TYPE DATA FULLY CORRECTED (Tim caught "lots of MAPD labelled MA-only"):** the `plan_type` field was badly wrong (82 MAPD plans mislabeled `ma`, incl. all D-SNP/C-SNP which ALWAYS have drug). Fixed via the **authoritative NC SHIIP MA Landscape** (`docs/Medicare Landscape Files/` / the Mecklenburg 2026 PDF): **"Annual Part-D Deductible = N/A" is the definitive MA-only signal — far better than name-parsing** (name-guessing missed BCBS Medical Only-134, Honor Giveback PPO-52, Freedom+-21). **True MA-only = 222 active members across 5 plans** (BCBS Medical Only H3449-012=134, Honor Giveback H5525-065=52, Freedom+ H3404-004=21, AARP Patriot No-Rx H5253-040=13, USAA Honor H5216-343=2); everything else with drug = MAPD (4,633). ⚠ Only Mecklenburg verified — other-county Landscape PDFs would close the last gap for plans not in that file. Many DB backups taken (`/root/founders_pre_*` — plantype_norm, network_snp, ma_mapd_fix, landscape_maonly, etc.).

**Reconciliation progress this session:** **HealthSpring ✅ 59→58** (termed Robert Owen, term_date 4/30 already set, ties to BOB). **Humana** — the "+174" was mostly a KEY-FORMAT ARTIFACT not real problems: matching on member_id-OR-humana_id-OR-MBI matched 2412/2445, 0 BOB-actives missing; applied 5 SAFE fixes (4 genuine terms w/ term_date + GROUP MANUAL junk) → **2445→2440**; **28 HELD** (legacy no-DOB/mangled-JR-name stubs where "not in BOB" is unreliable + 5 real-ID no-matches + 2 dup pairs — NOT bulk-termed).

**⚠ AETNA SWITCHERS — my earlier "4 switchers" was WRONG (DOB-only matching = false positives):** re-examined, 3 of 4 were unrelated people sharing a birthdate (Baker→"Sorrow", A.Lala→"Phillips", Lambert→"Grace"). Only **Barbara Overcash confirmed** (name+dob+EXACT address, Aetna Plus→UHC). **LESSON: switcher detection needs name+dob+address, NEVER dob-alone.** All Aetna SilverScript-Plus cleanup HELD for the cross-carrier pass.

**🆕 3 NEW FEATURES queued in BACKLOG (Tim's ideas 2026-07-13, all to do RIGHT not rushed):** (1) **Part-B Giveback filter** — needs authoritative Part-B rebate data first (name-parsing unreliable: HealthSpring Preferred Savings has a giveback but no "Giveback" in the name; givebacks come with AND without Rx). (2) **Secondary/retirement coverage flag on customers** (SHP teachers / TRICARE-VA-CHAMPVA military / cops-EMS-judges / FEHB postal — drives plan choice) — brainstorm→spec. (3) **Track non-commissionable PDP enrollments** (free Medicare.gov signups that leave no trace connecting them to Founders) — brainstorm→spec, biggest value.

**▶ NEXT SESSION:** when AJ delivers UHC/Devoted → reconcile them + the **cross-carrier switcher pass** (Overcash merge + re-detect switchers via name+dob+address + Humana 28 + coexistence dups). Then the per-agent×carrier Brian-view on clean data. Also queued: dedupe the 2 "Medigap Plan N" buckets (284/285); other-county MA Landscape PDFs to finish MA-only verification; the 3 new features. Quo 403 = missing QUO_WEBHOOK_SIGNING_KEY + QUO_API_KEY in VPS .env (Tim pasting).

---

## 🧭 (prev) START HERE — current state (updated 2026-07-10)

**▶▶ THIS IS THE ACTIVE PRIORITY (Tim, 2026-07-10): CARRIER-BY-CARRIER BOB↔DB RECONCILIATION to 100% accurate customers+policies.** Read `memory/session-handoff-2026-07-10-carrier-reconciliation.md`. Tim's method (the ONLY fool-proof one): for each carrier, diff the authoritative BOB (source of truth) against the DB line-by-line, active-only, and fix until they tie. Track BOTH **active policies** AND **active customers** (invariant: active policies ≥ customers; a person can hold >1 policy). This is the #1 thing the portal must do + it's the foundation of the Brian-view (do NOT build the per-agent×carrier plan-mix until data is clean — a pretty report on wrong data destroys Brian's trust).

**🔑 THE KEY METHODOLOGY (learned the hard way this session — apply to EVERY carrier):**
1. **Match on member_id OR MBI, BOTH formats.** The DB stores policies under two keying schemes (MBI-as-member_id from commission-import + carrier NG/MECH-format from BOB). Pure single-key matching gives false positives.
2. **MBIs CHANGE.** CMS reissues them (Carolyn Byrd's changed on a plan-switch). member_id is more stable than MBI.
3. **RECONCILIATION IS CROSS-CARRIER (the biggest finding).** A person active in carrier X's BOB but with a NEWER active enrollment in carrier Y has **SWITCHED** — carrier BOBs LAG on disenrollments (CMS batch-notifies weeks late + carriers keep pending-terms visible), so every BOB OVER-reports switchers. True active carrier = **newest effective date across ALL carriers**, not whoever's BOB still lists them. Switchers = stale row to term + duplicate customer records to merge. (Grounding: Barbara Overcash active in Aetna's BOB on SilverScript PDP but actually moved to UHC NC-0015 eff 1/1/26 — different MBI, same DOB/address. This is THE feature: the portal is the only system that knows a member's true current carrier by looking across all BOBs.)
4. **"In DB active, not in BOB" ≠ auto-term.** Could be: genuine term (has term_date), pre-Founders/off-Founders-book (Tim's Bradley/Clark — AOR=Tim, correctly not on the Founders BOB), OR a switcher. Each needs a REASON.
5. **BOB files come in TWO flavors — get the FULL export, not "visible rows".** UHC + Devoted current files are the LOSSY visible-rows download (no MBI / no member_id — name+DOB only → can't reconcile). Aetna/Humana/HealthSpring files ARE full (have MBI + member_id). ⚠ NEED FROM AJ before Wed: full UHC (w/ MBI) + Devoted (CSV w/ member_record_locator) + BCBS + Wellabe + GTL.
6. **Every BOB has a per-carrier STATUS column** (confirmed): Aetna=col R "A"/"T"; Humana=col AE "Active/Inactive Policy"; Devoted=Current Status "Enrolled"; HealthSpring=Status "Enrolled"/Disenroll-date; UHC=policyTermDate sentinel(2300-01-01)=active. Filter to ACTIVE before counting.

**SCORECARD (BOB active vs DB active, 2026-07-10):** **Aetna ✅ 95→79** (fully reconciled internal dups; 4 switchers + retype pending cross-carrier pass), Humana 2271/2445 (+174, ready — has full BOB), HealthSpring 58/59 (+1, ready), Devoted 510/33 (−477! but need full CSV — hugely under-imported), UHC 2184/2285 (need full export), BCBS ?/237 (need BOB).

**✅ AETNA DONE (this session, verified, money $2258.85 intact, 4 DB backups /root/founders_pre_aetna_*):** 95→79 active. Removed 14 phantom dups (9 MA/MAPD-twins where H3146-001-MAPD + H3146-004-MA = same plan; Bryant Value-Plus; **4 SilverScript-PDP mislinks** — a SYSTEMATIC bug where a PDP enrollment spawned a duplicate mis-linked Signature-HMO-MAPD phantom row, $4 PDP-sized pmt, MBI-keyed — WATCH FOR THIS ON UHC/HUMANA). Termed Burner (term_date set). Removed Byrd stale-orphan. **Fixed plan-type metadata: 15 Aetna buckets MA→mapd + Eagle H5521-241 mapd→MA → type mix now 69 MAPD + 10 PDP + 0 MA** (the "lots of MA-only showing up" bug = mislabeled buckets). **Created Don Long as non-login retired-agent User (id=18, email @retired.founders)** — Coker+Hill AOR→Don (were wrong-Rebekah via "Long" name-collision); Robert E. Smith got middle initial to disambiguate from other Robert Smith. **STILL PENDING on Aetna (cross-carrier pass):** term the 4 switchers (Overcash→UHC, Baker→Humana, A.Lala→Humana, Lambert→UHC) + merge their dup customer records → real Aetna ≈ 75 (73 + Bradley/Clark). Dixon + S.Lala = real Aetna PDP.

**✅ ALSO THIS SESSION — AEP LETTER GENERATOR fully spec'd** (`docs/superpowers/specs/2026-07-10-aep-letter-generator-design.md`, brainstormed + validated via mockups + a real one-page PDF `docs/AEP Letters/SAMPLE_Modern_AEP_Letter.pdf`). Agents pick plans→letter→send; a pay-per-piece bulk mailer prints+mails everything. Classic (Brian's design, recreated) + Modern (research-backed) letter styles; per-year campaign plan→letter map; 3-page send wizard w/ household de-dupe + per-row deselect + inline address write-back; Option-B compliance gate. **Vendor = OPEN (procurement, Tim+AJ):** Lob/PostGrid REJECTED (subscription floor / 500-mo cap); LetterStream rejected (double-window = too transactional/scammy for the personal touch). Need closed-face + live-stamp bulk mailer (Click2Mail/Addressers/PCM). AJ pulling last-year piece-count+cost baseline. TPMO disclaimer text deferred (Brian confirms). **This is spec-only, NOT built — a multi-phase build, won't be live by Wed.**

**NEW FEATURE queued (spec after UHC data): Retired-agent AOR + worklist** — Don Long created (id=18); still need Cynthia Mortimer + recover THEIR customers (re-parse raw Aetna/UHC commission files — writing-agent name isn't retained in the DB, only in `rollup.py`: 'donald long'/'cynthia mortimer'→Brian) + a "retired book" claim-worklist so active agents can legitimately get those orphaned customers onto their own book (customer gains a real servicing agent). **Money still rolls to Brian @50% via existing rollup — unchanged.** Fixes the Rebekah "Long" name-collision at scale.

**▶ NEXT SESSION:** (1) reconcile **Humana (+174)** + **HealthSpring (+1)** now (both have full ID-keyed BOBs) using the cross-carrier method; (2) when AJ delivers → reconcile **UHC/Devoted/BCBS** + do the **cross-carrier switcher pass** (term stale rows + merge dup customers across ALL carriers at once); (3) THEN build the **per-agent×carrier plan-mix Brian-view** on clean data + add a reconciliation integrity invariant; (4) **Aetna carrier PAGE redesign** — Tim picked FLAT & SORTABLE table w/ coverage-category filters (Part C only / PDP only / Medigap only). Also: **Quo webhooks 403** = missing `QUO_WEBHOOK_SIGNING_KEY` + `QUO_API_KEY` in VPS .env (Tim pasting keys → append + restart). Wed 7/15 11am Brian demo (Tim sent the email).

<details><summary>Prev state (2026-07-09) — orphan cleanup + crosswalks</summary>

## 🧭 START HERE — (updated 2026-07-09)

**✅ JUST SHIPPED + DEPLOYED + LIVE (2026-07-09) — per-carrier CROSSWALKS + Humana BOB ENRICHMENT + POLICY DEDUP; plan_id_orphans 316 → 112 on prod.** Read `memory/session-handoff-2026-07-08-orphan-cleanup.md` (has 07-09 addenda). This session finished the orphan whittle via the crosswalk pattern + big data-quality fixes:
- **Aetna crosswalk** (`crosswalk_aetna_plan_from_bob.py`, commits 270c2a7/232f10e): 51 orphans → 0, matched 100% by MBI (Aetna shares it) + `seed_pdp_buckets.py` (12 NC PDPs hand-maintained — CMS Landscape is MA/MAPD-only). Proved the per-carrier crosswalk pattern.
- **Humana crosswalk** (`crosswalk_humana_plan_from_bob.py`, 74411be/688aad3): 189 linked (48 by Humana ID + 141 by UNIQUE name; ANY shared name = ambiguous, skip — Tim's rule; persists Humana ID to `carrier_id_crosswalk` for self-heal). Humana orphans 226 → 41.
- **Humana name-parser fix** (normalizers `_humana_name`) — 'LAST SUFFIX FIRST' mangled 'Billy Morgan Jr' → stored 'Jr Morgan' + dropped the real first name; fixed + backfilled 13 (`repair_humana_mangled_names.py`).
- **Humana BOB enrichment** (`enrich_humana_from_bob.py`) — the FULL AJ BOB (85 cols + inactive rows, Inactive Date col AI) matched by Humana ID + unique name: filled 3,465 blank fields (dob-missing 177→35, gender 2455→47), backfilled 140 permanent Humana IDs, moved 15 MBIs to the right column, termed 11 genuine leavers. ⚠ **DRY-RUN CAUGHT A CRITICAL BUG**: naive term logic would have wrongly termed 103 AEP RENEWERS (old plan inactive 12/31 + new plan active 1/1 = still current) — fixed with a `has_active` guard (only term if NO active BOB row). This is why we dry-run money/book changes.
- **Policy dedup** (`dedupe_duplicate_policies.py`) — 92 customers had duplicate active policies for the SAME plan (Bobby Aderhold = 2 identical Aetna Value Plus). Merged 90 pairs → one each ($0 money change, 8025 pmts/$183,489.01 before+after). Identity model (Tim): **MBI = the PERSON (SSN-like), member_id = the POLICY — keep BOTH.** See [[policy-coverage-slot-model]].
- **DVH bucket** (`seed_dvh_buckets.py`) — Humana Extend 1250 (name-only; DVH benefit shape captured in [[add-plan-ui-design]] for the type-driven form).
- **Reconciled manually (Tim-verified):** John Murray (Humana→UHC switcher, merged dup + termed Humana), Pamela & Phil Rodgers (termed everywhere → left agency). Chase-list of the ~26 remaining: `docs/Humana_37_status_to_reconcile.csv`.

**plan_id_orphans baseline 316→112.** Suite 607. MISLINK 0. **REMAINING orphans (112):** ~26 Humana (mostly termed/left-agency, on the chase-list) + others; these are a STATUS-reconciliation to-do, not a plan-matching problem (they're not in the active BOB because they're termed).

**▶ NEXT (Tim's stated next brainstorm) — CUSTOMER PLAN-HISTORY + MEDICARE-ELIGIBILITY view** ([[customer-plan-history-eligibility]]). Modeled on HealthSherpa's CMS lookup: customer profile shows Part A/B effective + LIS + a plan-history timeline (current plan tagged + past chapters w/ plan name, type, full H-PBP-SEGMENT code, start/end). `customer_aor_history` ALREADY = the chapters (add cms_code+type + new customer fields Part A/B/LIS). Data-in = MANUAL (type-driven Add-enrollment modal = also the [[add-plan-ui-design]] Justin flow) + IMPORT; **NO CMS API** (Tim, solo dev). This ONE feature unifies [[policy-coverage-slot-model]] + [[plan-taxonomy-brian-view]] + [[bcbs-cms-plan-segments]] + [[add-plan-ui-design]]. **Brainstorm→spec.** Also still queued: one-active-per-category import validation; the 2 diff-eff-date dedup pairs (held for review); the ~26 Humana status chase.

</details>

**(prev, 2026-07-08 PM) ✅ CONTRACT-CODE LAYER 1 + plan_id_orphans WHITTLED 4,656 → 366 on prod.** Read `memory/session-handoff-2026-07-08-orphan-cleanup.md` first. Layer 1 shipped (merge `a6c3f05`, migration 035, 130 CMS buckets seeded/enriched, 4,063 linked). Then cleared orphans further: UHC aliases (38), out-of-state SC/TX/WV/VA (8; **state = a FILTER dimension NOT a bucket** — SC Humana Gold Plus ≠ NC one; `seed_plan_buckets --states` is white-label-ready), **BCBS mis-column recovery (193** — legacy BCBS had the plan NAME in the `plan_type` column from an old parser; `repair_plan_id_linkage._miscolumned_name` recovers it AND fixes the data), **medigap (88** — `find_plan_bucket` medigap branch now matches by letter at ANY year + alias-fallback; seeded Plan N / Humana G). 7 new tested idempotent scripts + `import_bob_file.py` harness. Suite 578, baseline ratcheted to 366, MISLINK guard 0, service healthy. ⚠ **A July-BOB re-import attempt FAILED + was reverted (near-no-op):** the 226 Humana blank-plan orphans are keyed by numeric commission PID, the Humana BOB by `Humana ID` → `_import_bob_row` can't match them (different ID namespaces); it refreshed 2,270 already-linked policies harmlessly + made 1 mislink that I NULLed back. **ALSO SHIPPED + LIVE (2026-07-08 PM) — carrier dashboard container reconciliation (Layer-2 start, commits `013ca4a`/`386dbe4`).** Investigating Aetna (Tim: "the 3 containers don't agree + linked plans aren't clickable") found the root cause: the Carriers drill-down grouped "Plans" by free-text `Policy.plan_name` and "policy-type mix" by free-text `Policy.plan_type`, and made a plan clickable only on an exact `plan_name` string-match — so a correctly-linked policy ("Aetna Medicare Value Plus (HMO)") whose bucket is named "Value Plus HMO" rendered NOT clickable, and the containers never reconciled. Fixed: `book_breakdown` now groups BOTH on the LINKED bucket (`Policy.plan_id`→canonical Plan) — by_plan keys off plan_id (clickable via id), by_plan_type derives from the bucket's clean type (casing canonicalized, MA≠MAPD kept distinct), unlinked policies → one honest "Unlinked / needs plan" row. All 3 containers now sum to the carrier total + agree. LIVE-verified on Aetna (125 total reconciles; every linked plan clickable). **The metrics now CONSUME the plan database.**

**▶ NEXT — two big designed pieces, both brainstorm→spec:** (1) **FINISH THE HUMANA CROSSWALK** (Tim's call) — the 226 Humana + 33 Devoted "Unlinked" orphans are NOT stubs (real customers whose `humana_id` holds the MBI); their plan flows THROUGH the crosswalk via the **name + last-6-of-MBI** bridge (PROVEN 33/45 0-collisions in [[commission-bob-crosswalk-diagnosis]] but NOT YET BUILT — a `confidence='mbi_last6'` tier); then repeat for BCBS/UHC/Devoted. (2) **PLAN TAXONOMY / BRIAN-VIEW** ([[plan-taxonomy-brian-view]], contract-code L3/4) — plans have MULTIPLE dims (coverage-tier Part C=MA+MAPD vs PDP vs Medigap vs DVH; drug MA-only vs MAPD; SNP C/D-SNP; network HMO/PPO/POS); Brian wants to query slices like "Aetna HMO C-SNP non-D-SNP" → total + plan + agent breakdown, carrier-sandboxed; build a shared plan-category helper. Also queued: state filter (NC default), add-plan UI ([[add-plan-ui-design]]), segment layer ([[bcbs-cms-plan-segments]]).

**(prev, superseded by the whittle above) ✅ CONTRACT-CODE PLAN DATABASE, LAYER 1 (buckets-first).** Merged to main (`a6c3f05`) + 2 post-deploy fixes; migration **035** applied on prod (034→035, head 035; `Policy.contract_code`/`plan_year` + `Plan.needs_review`, needs_review backfilled false on all 41 existing plans); deployed + restart cycled 20:12 UTC, login 200, no errors. **What it is (Tim's jelly-bean model):** the `Plan` table is the authoritative set of NC plan "buckets" seeded from the CMS CY2026 Landscape; the BOB importer SORTS each policy into an EXISTING bucket (`app/plan_bucket.find_plan_bucket`) and NEVER auto-creates on a guess (a miss leaves plan_id NULL for review). New: `scripts/seed_plan_buckets.py` (CREATE buckets), `app/plan_codes.py` (classify/extract sorting keys), `app/plan_bucket.py` (sort-into-existing), wired into live `_import_bob_row`, `scripts/repair_plan_id_linkage.py` (one-time backfill). 5 TDD tasks + per-task reviews + **opus whole-branch review** (caught 2 blockers: cross-year alias mis-link + word-boundary medigap keywords — both fixed `3b4741f`). **LIVE DEPLOY RAN (DB backups `/root/founders_pre_layer1_contractcode_*` + `_pre_seed_cleanup_*`): seeded 130 buckets (dash-form) + enriched 96 via sync_cms_plan_data + repair linked 4,063 orphaned policies → `plan_id_orphans` 4,656→642 (baseline ratcheted).** MIS-LINK GUARD verified 0 (no year_bound policy linked to wrong-year bucket). ⚠ **POST-DEPLOY BUG CAUGHT + FIXED LIVE (`1122ffc`):** seed stored `cms_plan_id` in CMS underscore form (`H1036_167`) but the whole system matches DASH form (`H1036-167`) → all 130 buckets were orphaned from the sorter (only 26 dash-form pre-existing plans enriched). Root cause: unit-test fixture used dash, never exercised real underscore data (the SQLite-hides-Postgres-reality class CLAUDE.md warns about). Fixed + regression test + re-seeded (0 underscore, 136 dash). **REMAINING 642 orphans:** 608 have NO plan_name (need BOB re-import), ~34 UHC/BCBS friendly names await reviewed aliases (deferred). **NEXT: (1) seed reviewed UHC/BCBS aliases + re-run repair to catch the ~34; (2) the SEGMENT LAYER (Layer 6) — [[bcbs-cms-plan-segments]]: BCBS/HS/Humana plans have per-county segments (BCBS Essential Plus H3449-023 = 4 benefit sets; Mecklenburg=seg1/Union=seg2/Cabarrus=seg4, ~50-75% of Tim's book); Tim wants "one plan, segment as sub-detail" + per-segment PBP benefits. Layer-1 lumps them at 2-part grain (correct grain, NOT a bug); segment detail is the next build.** Layer 2 (single-source metrics + guard) also queued.

**(prev) 🧭 read `memory/session-handoff-2026-07-06-plan-database.md`.** Summary:

**✅ SHIPPED + DEPLOYED + LIVE (2026-07-06) — a batch of commission/BOB/stub fixes (all on `main`, prod @3af5793, migration head 034).** (1) **Humana carrier ID crosswalk + 45-stub cleanup** (merge 8e0a9d1, migration 034): `carrier_id_crosswalk` GrpNbr↔customer↔MBI; name+eff cleanup tier merged 45 legacy Humana stubs (non-Jan-1 eff + unique-name + not-shared-by-3+; PROVEN 0 collisions); **stubs 540→495**; opus caught 2 Postgres-only Criticals (fixed). (2) **3 BOB July-format parser fixes** (Aetna=split cols; UHC=agent-centric no-MBI→synth UHCND-id; Devoted=Application-Status no-id→synth DVND-id) — AJ's 3 uploads were erroring, now work (he uploaded UHC 2182/Devoted 505/Aetna 211). (3) **orphan-PREVENTION fix (74eb392)** — the old D-11 guard was orphaning no-MBI BOB rows; now only quarantines rows with no id AND no name. (4) **merge_customers MBI-donation fix (3af5793)** — nulls donor MBI to avoid ix_customers_mbi collision. (5) **Wellabe consolidation** (Medico+Wellable→one "Wellabe"). (6) **Healthspring imported** → 8/9 stubs auto-resolved. ⚠ **REGRESSION I CAUSED + FULLY UNDID:** an orphan-backfill made 2,687 DUPLICATE customers (no-MBI → resolve_customer created new) — deleted all, back to 216 dups, money intact. **LESSON: no-MBI carriers must MATCH-existing (name+DOB) FIRST, never blind-create.** LIVE stubs: BCBS 206 (needs BOB), Humana 179, UHC 69, Devoted 31, Aetna 1, Healthspring 1. **STILL NEEDED FROM AJ: BCBS / Wellabe/Medico / GTL BOBs.**

**🔴 IN FLIGHT (DESIGNED, Layer-1 T1 built, NOT deployed) — CONTRACT-CODE PLAN DATABASE (the Brian-view).** Brian (controls Tim's pay) wants customers counted by CONTRACT CODE not plan name, filterable every way. **8-layer spec** `docs/superpowers/specs/2026-07-06-contract-code-plan-database-design.md`; **Layer-1 buckets-first plan** `docs/superpowers/plans/2026-07-06-contract-code-layer1-data-foundation.md` (T1 migration 035 DONE=42b80a4/suite 533; T2-5 pending). Branch **`feat/contract-code-layer1`** (6 commits, unmerged). **KEY DECISIONS:** JELLY-BEAN/buckets-first (seed ~187 NC CMS buckets FIRST, parser SORTS into EXISTING, NEVER auto-creates, miss→review queue); plan identity TYPE-DEPENDENT (year-bound=code+year / medigap=letter@PERPETUAL=0 / DVH-named@PERPETUAL); plan_year=BOB-snapshot ≠ eff_date; `seed_plan_buckets` CREATES + `sync_cms_plan_data` ENRICHES (complementary); UHC/BCBS BOBs have NO code→alias-match (UHC BOB can be regenerated w/ code cols); L7 pharmacy-network is CUSTOMER-FIRST tie-breaker-only. **NEXT: resume subagent build from Task 2 (seed_plan_buckets).** This IS remediation item-3 (plan_id linkage, 4611 orphans), done right. Deploy human-gated.

<details><summary>Earlier state (2026-07-02) — customer name normalization + preferred name + merge UI (SHIPPED)</summary>

**✅ JUST SHIPPED + DEPLOYED + BACKFILL APPLIED (2026-07-02) — CUSTOMER NAME NORMALIZATION + PREFERRED NAME + MERGE UI.** Merge `983dfea` (+ login hotfix `fa8e0fb` + MI-recovery `b8505ca`/`43e6f15`); migration **033** (preferred_name) applied, head 033; deployed + restart cycled 19:41 UTC, login 200, no errors. Suite 489+. **Why:** item-2 shipped but "John Couchell ×4" wouldn't collapse — his stubs (`COUCHELL, JOHN`, blank first/last) couldn't be FOUND because customer names were stored inconsistently (496 drift / 313 ALL-CAPS / 252 comma / 44 blank-first across 5,146). **What shipped:** (1) a `before_insert/before_update` event on `Customer` keeps `full_name`=first+last across all ~7 write paths — NOT a freeze (humans edit first/last freely; blank-first stub keeps its raw full_name). (2) **`preferred_name`** column + `address_as(customer)` seam (preferred if set else legal first) for conversational greetings — legal name Donald, greet as Craig; editor + "goes by" hint on the profile; preserved through merges (`_MERGE_FILL_FIELDS`). address_as retrofit is INCREMENTAL (labels=legal mailing name, SMS=free-text today; adopt as found). (3) `scripts/normalize_customer_names.py` (dry-run/--apply, skips manually_edited, idempotent). (4) rich per-row context (DOB/MBI/policies/carriers/source/agent) in the `/admin/customers/duplicates` merge UI. **8 TDD tasks + per-task reviews + opus whole-branch review** (traced the global event clean vs all 7 name-write sites; caught a latent backfill I-1 = folded-MI + dirty-last, fixed). **BACKFILL APPLIED (DB backup `/root/founders_pre_name_normalize_20260702_192742.sql`): 589 names normalized, 248 preserve their middle initial** (Tim's disambiguation req — Beaver ×3, Barringer ×2, Parisi keep their MI, via BOTH comma-form "Beaver,Colleen E"→"Colleen E. Beaver" AND space-form "Claudia S. Parisi" recovery; strict single-letter same-person gate). Idempotent (re-run=0), 0 corrupt. **Couchell stubs → "John Couchell" now searchable + surface as 2 `name_only` clusters (John keeper 1366, Aphroula keeper 1365) mergeable in the UI.** ⚠ **LOGIN 500 ROOT-CAUSED + FIXED (fa8e0fb):** OAuth callback created NEW users WITHOUT `agency_id` (NOT NULL) → 500 for any first-time login (Michael hit it); fixed via `_get_or_create_oauth_user` setting `agency_id=DEFAULT_AGENCY_ID`; also renamed his prod row mike@→michael@ (id=7); Michael confirmed IN. Specs/plan `docs/superpowers/specs|plans/2026-07-02-customer-name-normalization-and-merge-ui*`. **NEXT: UI-merge the Couchell + other `name_only` clusters (human confirm-checkbox in /admin/customers/duplicates); then back to data-integrity remediation ITEM 3 (plan_id linkage, 4611 orphans).**

</details>

**✅ JUST SHIPPED + DEPLOYED + LIVE (2026-07-01) — DATA-INTEGRITY REMEDIATION ITEM 2: NO-MBI CUSTOMER MERGE.** Merge `258bd62` + AOR-collision hotfix `b061625` + baseline ratchet `a30c4e2`; deployed to VPS (restart cycled to 19:37 UTC, login 200, `/admin/integrity` 302, no errors); no migration (head still 032). **What it does:** collapses duplicate-customer clusters into ONE profile EVEN WITH NO MBI (the old `customer_merge` only caught MBI dups). `app/dedup.py find_no_mbi_clusters(agency_id)` clusters by NORMALIZED `full_name` (so blank-name commission stubs like "CONNELLY, JOHN" cluster with the real "John Connelly") + tags each cluster `conflict`>`dob_match`>`shared_id`>`name_only`. `merge_customers(keeper, losers, agency_id, actor)` reattaches Policy/CommissionLineItem/notes/contacts/AOR + PolicyPayment (transitively via Policy) + MatchSuggestion (repointed); **fill-blanks-only** (manual>real>stub, never overwrites keeper); **refuses contradictory dob/mbi**; idempotent; audited. `/admin/customers/duplicates` now shows no-MBI clusters with **signal-gated merge** (conflict→blocked, name_only→required confirm checkbox, dob_match/shared_id→normal). **Remediation #4:** editing a customer's MBI to one already used returns a 409 merge-offer (no hard error, MBI not saved). `scripts/merge_no_mbi_clusters.py` (dry-run/--apply, dob_match+shared_id only). **7 TDD tasks + per-task reviews + opus whole-branch review — opus caught a merge-blocking Postgres-only Critical (missing CommissionLineItem/MatchSuggestion reattach → FK-500 on the exact Connelly case, SQLite-invisible) + an I1-residual (agency filter would re-orphan NULL-agency policies); the LIVE `--apply` then hit a SECOND real bug (AOR unique-constraint collision on shared enrollment chapters) → hotfix `b061625` collapses duplicate AOR chapters instead of colliding.** Suite 457. **LIVE CLEANUP RAN (DB backed up `/root/founders_pre_item2_no_mbi_merge_20260701_162704.sql`): 12 dob_match clusters, 28 rows→12 keepers, 0 skipped, 0 orphans (verified). John Connelly ×5→1 (keeper 1367 keeps MBI+DOB+5 policies); the 3 OTHER real Connellys (Carolyn/Richard/Deborah) correctly NOT swept in.** Radar: **`duplicate_customers` 18→5, `orphan_stub_customers`/`commission_import_stubs` 571→556** (baselines ratcheted). **`payment_without_customer` stayed 88 — VERIFIED pre-existing (the 06-29 UHC-quirk +12, baseline kept 76); merge did NOT touch it (0 line items nulled, proven against the backup).** Specs/plan `docs/superpowers/specs|plans/2026-06-30-no-mbi-customer-merge*`. **Deferred→BACKLOG:** the ~5 remaining `duplicate_customers` + 277 `name_only` loose clusters (UI review, not auto); M1 keeper-tiebreak doc, M2 per-agency audit actor.

**✅ JUST SHIPPED + DEPLOYED + LIVE (2026-06-30) — ROADMAP & CHANGELOG + BUG INTAKE** (merge ae9aa96 + title-blank fix 2bf8bd8; migration 032 applied 031→032; restart cycled 02:33 UTC; login 200, /roadmap 302, no errors; 12 history items seeded). A `/roadmap` board (Planned/Known · In progress · Shipped) of expand-in-place cards showing "what the issue was → the fix". **Everyone sees the same shared board** (any agent submits a bug, visible to ALL immediately = anti-duplicate); admins triage (status/priority/edit) + **dismiss** (status='dismissed' → off the shared board, still in the submitter's `?mine=1` view). `RoadmapItem` model (no visibility column; `_HIDDEN_STATUSES={wont_fix,dismissed}`; `column` property = the ONE status→column mapping) + `roadmap_bp` (`app/roadmap.py`: board read, submit, admin edit/dismiss — all agency-scoped, edit `abort(403)`-before-lookup) + `roadmap.html` (Founders theme, expand-in-place, admin-gated controls, XSS-clean autoescape) + nav (admin+agent) + `scripts/seed_roadmap.py` (idempotent on agency_id+title, dry-run/--apply). 6 TDD tasks + per-task reviews + **opus whole-branch review** (confirmed clean multi-tenant isolation/privilege/XSS; caught + fixed an admin blank-title→500 NOT-NULL bug the per-task reviews missed). Suite 443. **Tim's next step: curate/edit the 12 seeded entries' wording in the admin UI as desired + add more.** Specs/plan: `docs/superpowers/specs|plans/2026-06-29-roadmap-changelog-bug-intake*`. **Deferred (→BACKLOG):** portal-wide CSRF (the whole app has none — OAuth+SameSite=Lax baseline; roadmap matches the existing pattern); admin-edit enum allowlist for type/status/priority.


**✅ JUST SHIPPED + DEPLOYED + LIVE (2026-06-26 PM) — ITEM 1: STUB-CREATION PREVENTION (commission = match-or-park).** Merge `ab7dde8` + invariant hotfix `9ec41f6`, deployed to VPS, restart confirmed cycled (ActiveEnterTimestamp advanced), login 200, `/admin/integrity` healthy, no errors. **What it does:** commission import NO LONGER creates customers — it attaches a payment to an existing customer ONLY by unique carrier ID (MBI/carrier_member_id/humana_id), else PARKS it (`PolicyPayment.policy_id=NULL`, `match_confidence='unmatched'`) for BOB/human resolution. **BOB is now the ONLY door that creates identity.** Parked payments AUTO-SWEEP onto a customer when a later BOB import supplies the matching ID (`sweep_parked_payments` in payments.py, wired in `_upsert_customer_from_policy`). Commission names normalized to "First MI. Last" (`normalize_person_name`). Unsupported-carrier uploads BLOCKED (`_carrier_supported_or_reason`, keys off NORMALIZERS — catches Wellable which is in PARSERS but not NORMALIZERS). Two radar invariants: `commission_import_stubs` (ratchet 571→0 as item 2 cleans) + `statement_balance_complete` (the ledger's INTERNAL balance Σraw==Σpayout+Σkeep — NOT lineitems-vs-PolicyPayment; the deploy caught that mistake live on Devoted/Healthspring and it was hotfixed). 7 TDD tasks + per-task reviews + opus whole-branch review (caught the Humana sweep bug + NON_CUSTOMER exclusion + name-norm regression — all fixed). Suite 419, no migration. **⚠ "park = no payout" is NOT enforced this merge** (payout flows from CommissionLineItem regardless; Tim's call — corrected the wording, split enforce-no-payout to BACKLOG). Pre-deploy DB backup `/root/founders_pre_item1_stub_prevention_20260626_203806.sql`.

**✅ ALSO SHIPPED + DEPLOYED + LIVE (2026-06-29) — 3 of the 4 UHC quirk hot-fixes** (merge f81e912 + review-fix b197527; restart cycled to Jun29 15:40 UTC; login 200, fidelity 302, no errors; opus whole-branch review caught + fixed an undo re-orphan path). **(1)** UHC HA/HRA now attributes by the SOLICITOR agent ID in the HA action string (`_uhc_ha_agent_id`, mirrors the DVH twin), not the unreliable col-5 name → re-resolves agent + rate (Luz HA → Michael 0.525 not Rebekah 0.55); `edit_line_split` now PRESERVES `hra_bonus` (was hard-coding AGENT_COMMISSION → HRA mislabeled Renewal). **(2)** PARTD $4.59 renewal SPLITS at the agent's rate (plan-type carve-out before the $4.59-override branch); MA-family $4.59 + $0.26 PARTD untouched (Pinkston $4.59 → agent $2.52/Founders $2.07 @55%). **(4)** override `::ovr` siblings inherit `customer_id` on create/update/UNDO (was orphaning line items) + backfill RAN (`scripts/backfill_override_sibling_customer.py --apply`: 24 orphans, 12 fixed, 12 skipped=parent-also-customer-less). Suite 426, no migration, **no money-total change** (only classification/attribution/customer_id move). **⚠ Deploy was CODE + BACKFILL ONLY — May UHC NOT re-imported** (Tim's call: preserves AJ's 10 UHC + 2 Humana manual_adjusted edits, confirmed intact). Quirk #1/#2 apply to FUTURE uploads; existing May UHC auto-corrects on a LATER deliberate re-import (once manual-edit preservation lands; #1/#2 make Luz/Pinkston edits moot).

**✅ ALSO SHIPPED + DEPLOYED + LIVE (2026-06-29) — quirk #3 (Fidelity perf + filter persistence)** (merge 50a7f4a + review-fix 33365bc; restart cycled 16:11 UTC; login 200, fidelity 302, no errors). The UHC Fidelity view (~4k rows) choked because the template rendered a hidden full edit-form PER row (~4k forms + ~40k `<option>`s). Now: data rows only + ONE shared agents `<template>`; the edit form is built in JS on Edit-click (lazy). Edits save via **AJAX** (`commission_line_edit` returns JSON via the new `fidelity_row()` helper extracted from `fidelity_view`) and repaint the row + its `::ovr` sibling + the per-section footers + the top totals IN PLACE — no reload, which **also fixes the agent-filter reset**. Filters **persist to the URL**. **Browser-verified (Playwright, seeded 3,981-row stmt):** 0 per-row forms, 12 `<option>`s, ~48k DOM nodes (was ~120k+), no navigation on save, row+footer+totals stay consistent, edit persists. Opus review caught 2 display-drift bugs (stale footers + sibling row not repainted) → fixed + re-verified. **No change to `edit_line_split` or commission math.** Suite 428, no migration. **(The Gemini deferred-render stash is now obsolete — the real fix shipped; the stash can be dropped.) ALL 4 UHC QUIRKS NOW LIVE.**

**▶ RESUME HERE — data-integrity remediation ITEM 3 (plan_id linkage repair) — BRAINSTORM-FIRST.** Items 0 (radar), 1 (stub prevention), 2 (no-MBI merge) all SHIPPED + LIVE. **Item 3 goal:** repair the **`plan_id_orphans` 4611** — ~90% of policies aren't linked to a `Plan` row (the 250-vs-1701 count bug), so plan-level counts ≠ carrier counts. Resolve `Policy.plan_id` via the existing `_plan_alias_map`/friendly-name matching; drive `plan_id_orphans` toward 0 + ratchet the baseline. Roadmap: `docs/superpowers/specs/2026-06-25-data-integrity-remediation-roadmap.md` (items 3-7 open: 3 plan_id, 4 hub resolve actions, 5 count consistency+guard, 6 lead lifecycle, 7 AOR-is-per-policy). **LIVE RADAR DEBT now (post item-2):** plan_id_orphans 4611, orphan_stub_customers/commission_import_stubs 556, payment_without_customer 88 (RED, +12 = 06-29 UHC edit-orphans, NOT item-2's — identity-recovery's call), no_name_policies 56, **duplicate_customers 5** (down from 18; the ~5 remaining + 277 `name_only` loose clusters await UI review — NOT auto-mergeable), statement_balance_complete 0. **ITEM-2 FOLLOW-UPS (BACKLOG):** UI-merge the remaining 5 duplicate_customers + triage the 277 name_only clusters in `/admin/customers/duplicates`; M1 keeper-tiebreak docstring, M2 script per-agency audit actor. KEEP DOING: spec→plan→subagent build + **opus whole-branch review on every data path** (it caught a Postgres-FK-500 AGAIN this round — AND the live `--apply` surfaced a 2nd real bug SQLite hid, the AOR unique-constraint collision; lesson: real-Postgres apply IS part of the test) + DB backup + real-Postgres verify + confirm restart cycled + **all times EST/EDT (DB is UTC)**.

---

**(SUPERSEDED — kept for history) ✅ SHIPPED + DEPLOYED + LIVE (2026-06-26 AM) — DATA-INTEGRITY RADAR (roadmap item 0).** Tim's core worry — "we fix one thing and break two; pages show different numbers; is the portal FUBAR?" — now has a system answer. Built spec→plan→subagent-build (7 tasks)→opus-whole-branch-review→deploy. **What it is (NOT an auto-fixer — machine finds+counts+prevents-regression, human makes truth calls, tools execute approved fixes):** `app/integrity.py` = ONE `@invariant` registry of data-truths, consumed identically by a CLI (`scripts/audit_integrity.py`), the `/admin/integrity` dashboard (admin-only), and a CI **baseline-ratchet** guard (`tests/test_integrity_guards.py` + `integrity_baseline.json`) that **fails the build only if a violation count rises ABOVE its frozen baseline** — existing debt doesn't block, nothing gets worse, each fix ratchets a baseline toward 0. **This is the mechanism that ends the whack-a-mole.** Covers data + cross-page-consistency + route/page-health invariants; **lifecycle-aware** (a `source='manual'` lead with no MBI is VALID, not a violation) + **multi-AOR-aware** (a person with 2 concurrent AORs is ONE customer, not a dup). Absorbed + replaced the old `test_metrics_guard`. Opus review caught 1 minor (NULL-dob name-only false-positive) → fixed. **LIVE PRODUCTION DEBT (the frozen baseline = the to-do list, items 1-5 drive each to 0):** plan_id_orphans **4611** (90% of policies unlinked to a Plan — the 250-vs-1701 bug), orphan_stub_customers **571**, payment_without_customer **76**, no_name_policies **56**, duplicate_customers **18** (high-confidence; NULL-dob name matches excluded); backwards_date/carrier_agree/count_via_metrics/links_resolve **0**; no_orphan_routes 7. DB backed up pre-deploy. See `docs/superpowers/specs/2026-06-25-data-integrity-radar-design.md` + `…-remediation-roadmap.md`.

**▶ RESUME HERE — the data-integrity remediation roadmap (`docs/superpowers/specs/2026-06-25-data-integrity-remediation-roadmap.md`), now that the radar (item 0) is live.** Build order: **1.** stub-creation prevention (stop the import spawning dup customer stubs — John Connelly = ONE person split into 5 rows; Tim confirmed) → **2.** no-MBI customer merge (collapse the dup clusters; also fixes "can't edit a used MBI") → **3.** plan_id linkage repair (the 4,611 orphans → plan counts == carrier counts) → **4.** hub resolve actions (needs-match/needs-name actionable) → **5.** count consistency + guard (members·policies labeled, everything via metrics) → **6.** lead lifecycle (stage-driven, auto-advance on eff date) → **7.** AOR-is-per-policy (retire single primary_agent_id; Rebekah+Tim same customer; likely a dup-stub root cause). Each item = clean its radar invariant to 0 + ratchet the baseline down. **NEXT ACTION: brainstorm item 1 (stub-creation prevention).** Still-open older: Round 2 reconciliation (UHC 305-vs-262), AEP same-eff tie-break (~Oct 2026), per-enrollment `policy_uid`. KEEP DOING: spec→plan→subagent build + per-task review + **opus whole-branch review on every data/money path** + DB backup + real-Postgres verify + **confirm `systemctl restart` actually cycled (ActiveEnterTimestamp advanced)**.

---

**✅ SHIPPED 2026-06-25 (commit f320232) — Chronological BOB dedup fix (Robbie Belk + the 13).** The bug turned out to have **THREE layers**, all fixed via spec→plan→subagent-build→opus-whole-branch-review→deploy→live-verify: (1) `_dedupe_bob_records` blind last-wins → now collapses ONLY active rows, chronologically (`_rec_is_more_current`, **TERM-DATE-FIRST**: un-termed/sentinel-term beats a real past term; then later term; then later effective; full tie → last-in-file = UHC parity unchanged); termed rows pass through to plan-history. (2) the termed-rec router termed a member's NEWER active policy because the old + new rows share member_id → guard added; extracted `_route_termed_rec` now SHARED by `/upload/bulk` AND legacy `/upload` (the duplicate copy was itself a found bug). (3) **opus-caught third layer**: `_close_open_aor_on_term` closed the LIVE AOR interval at the OLD term date → one `is_current_enrollment` decision now governs the policy-term AND the AOR-close. **Live result on Postgres:** Robbie Belk (6274) = active C-SNP eff 2026 + agent_id=6 + closed Value Plus history chapter; **Aetna 74→87 active, 15→2 termed** (the 13 flipped back); UHC unaffected; idempotent (0 dup policy groups). One-time `scripts/repair_backwards_aor_intervals.py --apply` reopened 1 residual backwards interval (Katherine D. Bryant — re-import can't self-heal those). DB backed up pre-deploy. **Known deferred gap (BACKLOG):** the AEP same-effective-date tie-break (2 new plans both eff Jan 1 → last APP submitted wins; needs the parser to capture `Application Signed Date`; trigger ~Oct 2026). **Long-term BACKLOG item (Tim's call):** a per-enrollment surrogate `policy_uid` (Zoho-style) so reused member_ids stop conflating enrollments — the structural fix that removes this whole bug class.

**▶ RESUME HERE — back to the two brainstorm-ready specs (no open build thread):** **Round 2 = date-aware BOB↔commission reconciliation engine** (the "do my book and my pay agree" solve; grounding = Tim's UHC 305-vs-262; the dashboard's "N to reconcile →" is its front door; also owns "newer-wins" / BOB-authoritative-by-date). **Round 3 = whole-codebase audit & map** (`/gsd:map-codebase` + route-integrity test + orphan-page test). Both are BACKLOG stubs under 💰 Commissions. KEEP DOING: spec→plan→subagent build + per-task review + **opus whole-branch review on every data/money path** (it caught the AOR layer THIS round, after per-task reviews missed it) + DB backup + real-Postgres verify.



**NEWEST (2026-06-23) — BOB DATA-INTEGRITY MARATHON: 4 full spec→plan→build→opus-review→deploy cycles, all merged to main + LIVE on VPS + verified on real Postgres.** Full detail in `memory/session-handoff-2026-06-23-bob-data-integrity.md` (read it first). Summary:
1. **Agency Metrics + Attribution** (`…/2026-06-20-agency-overview-attribution-metrics*`) — **`app/metrics.py` = the SINGLE source of truth for agency book/money** (book from Policy, money from the CommissionLineItem ledger via `split_breakdown` — NEVER from counts) + a **build-failing guard test** (`tests/test_metrics_guard.py`) if a route computes book/money elsewhere; honest dashboard (fake MAPD/55% estimate DELETED); **`/carriers/c/<carrier>` drill-down** + Brian **agency/own toggle**; `app/branding.py` colors; writing-ID→agent resolver (`app/attribution.py`). **Backfill RAN: Brian 481→1,216, coverage 99.1%; then customer-agent propagation → unassigned 2,833→38.**
2. **Identity Recovery** (`…/2026-06-22-policy-payment-identity-recovery*`) — the "everything traces to the AOR" guarantee: four links (payment→customer, policy→name, customer→agent, owned→AOR interval) or the **Needs Identity hub** (repurposed `/customers/unassigned`, 4 categories). `_match_by_carrier_member_id` resolved **393 payments** (it was a missing join), `app/identity.py` name recovery (carrier-scoped), **2,350 AOR intervals derived**. Hub residual 122.
3+4. **Aetna parser** (`…/2026-06-23-aetna-bob-parser-rewrite*` then `…-aetna-csv-bob-plan-history*`) — XLSX rewrite (was reading non-existent cols → agent 0%) + `app/names.py` "First MI. Last"; then the June **MedicareApprovedBOBReport CSV** (DOB/phone/address freshness) with the **§6b termination→close-open-AOR lifecycle** (now "terminations close the AOR like normal" going forward) + **§4.2 add-only plan-history** (closed CustomerAorHistory chapters → the carrier/plan timeline on profiles, e.g. Danny Watts closed-Aetna→open-UHC). **RAN: Aetna attribution 58→74 (100%), 9 plan-history chapters LIVE.**

**⭐ NEXT (both BACKLOG stubs, ready to brainstorm):** **Round 2 = date-aware BOB↔commission reconciliation engine** (stale-BOB-as-of labeling, saved-customer/CMS-lag chargebacks, per-agent BOB freshness, "newer-wins"; grounding case = Tim's UHC 305-vs-262; the dashboard's "N to reconcile →" is its front door). **Round 3 = whole-codebase audit & map** (`/gsd:map-codebase` + route-integrity test + orphan-page test). KEEP DOING: spec→plan→subagent build + per-task + **opus whole-branch review** on data/money paths (it caught a real bug EVERY round) + DB backup + real-Postgres verify.

**FIRST ACTION each new session:** read these memory files before doing work — they hold current state that this file summarizes:
1. `memory/session-handoff-2026-06-23-bob-data-integrity.md` — NEWEST (above). The 4-round BOB data-integrity marathon, all LIVE; Round 2/3 next.
2. `memory/session-handoff-2026-06-19-commission-trust.md` — prev (still-open, NOT superseded): **COMMISSION TRUST SYSTEM.** Plan 1 audit-trail+undo+edit LIVE (mig 029), A3 balance GATE (mig 030), A2 Fidelity View, UHC New Comp-Type-R auto-split. **Still-open NEXT: re-upload preservation of manual edits (⚠ do NOT re-upload a statement after hand-editing — manual edits aren't preserved through re-parse yet) → A8 carrier stated-total cross-check → A9 Brian read-only dashboard → A1 multi-file upload / A7 lock.** **BLOCKED on AJ (do NOT guess):** UHC Comp-Type-I New rule + New-Chargeback rule + Betty's model + ~5 rare Humana codes. **⬜ fix-before-AEP:** UHC New proration `13−eff.month` ignores YEAR.
2. `memory/uhc-parser-resume.md` — UHC parser details + resume.
3. `memory/m2-phase2-resume-point.md` — M2 UI Phase 2 resume (Phase 1 shipped: design system + login + spinner all live).
4. `memory/roadmap-2026-06-09.md` — the active roadmap.
5. **`BACKLOG.md` (repo root) — the LIVE backlog of what's open, by category.** Skim ⚠ Regressions + 🔴 items first. Keep it current per the Session Protocol (this is the anti-drift doc — unfinished items live here, not in Tim's head).
Then skim the relevant spec in `docs/superpowers/` (UHC: `2026-06-11-uhc-raw-parser-design.md`; carrier models: `docs/Commission DL/_organized/CARRIER_MODELS_NOTES.md`).

**Where we are (2026-06-11 end):** Security milestone done (S0/S1/S2 ✅, S3 deferred). **M2 Phase 1 UI SHIPPED** (design system tokens + rebuilt login w/ compounding-pestle logo + OAuth spinner — all deployed/live). **Commission carrier reconciliation in progress (parallel R4 work):** UHC raw parser built+validated (97.7% auto, all agents balance to the penny, pushed, NOT wired live — remaining: "New" enrollment math + wire-up). Commission docs reorganized into `docs/Commission DL/_organized/` (raw/ vs aj-computed/ per carrier; originals archived). **Devoted ✅ FULLY VALIDATED 2026-06-11:** Brian's split=0.50 was already correct on the VPS (no change needed — note was stale); re-validated to the penny (agency $9,824.99 + Rebekah −$342.18 both balance Σraw==money_rows_total); Cyndi/Don retired-agent rollup N/A for Devoted (not in the April raw file). Our parser caught an AJ hand-math error (Chris $722.97 vs AJ's $695.47 — AJ missed an HRA). **Aetna ✅ RESOLVED 2026-06-11 via RETIRED-AGENT ROLLUP:** the "-$18" was Donald Long (retired, not a portal user → his rows fell to the 0.55 agency fallback instead of his real 0.50). New `app/commission/rollup.py` `apply_rollup(name, carrier)` rewrites Cyndi Mortimer / Donald Long → Brian Freeman for **Aetna + UHC ONLY** (per Tim), wired at all 3 name→agent seams (split-rate, line-item resolver, PolicyPayment). Verified on real data: Don $36.92→Brian@0.50=$18.46 (=AJ's "$36.92/2"); total $514.92→$513.07. Commit 6b7a0bb, 207 tests green (NOT yet deployed — no migration; `git pull`+restart, then re-upload May Aetna to apply). **UHC ✅ SHIPPED LIVE + IMPORTED 2026-06-12 (commits ac0d5e3, 7deb8a5, c1ba8c2; migration 026 applied to VPS):** flows through the normalized pipeline (recap + ledger + customer sync + rollup) like the other 5 carriers; new `normalize_uhc` + quarantine tab (`/admin/commissions/<id>/quarantine` + '⚠ N review' badge). **UHC May 2026 is ALREADY IMPORTED on the VPS** (statement id=55, 3,944 line items, Σraw $70,466.86 to the penny, 89 quarantined (2.3%), status=verified; Brian recap $4,765.85 = own+Don+Cyndi rolled up; Betty via alias; 0 duplicate-MBI customers). **Real-file check caught 2 split bugs (fixed):** Cyndi's name is 'MORTIMER, CYNTHIA WALKUP'→'cynthia mortimer' (rollup key); Betty writes as 'RIDDLE, BETTY B'→`_NAME_ALIASES` 'betty riddle'→'betty marlowe'. **TWO PRODUCTION HOTFIXES from the first real uploads (both deployed+verified on Postgres):** (1) migration **026** widened `commission_line_items.payment_type` 32→256 (quarantine rows store the full DVH action string, up to 145 chars — `StringDataRightTruncation`); (2) commit c1ba8c2 added `db.session.no_autoflush` to the resolver's 3 match-read helpers — a re-upload hit `UniqueViolation` on `ix_customers_mbi` because a match SELECT autoflushed a pending stub INSERT (Postgres partial-unique-index bug INVISIBLE to the SQLite test suite). Re-upload now proven idempotent on Postgres (customers_created=0). 221 tests. **REMAINING: AJ eyeballs a real UHC run + the quarantine tab before system-of-record; 'New' enrollment proration stays quarantined.** **KNOWN GAP (deferred):** Aetna Medigap *supplement* (`aetnasupplement.zip`, $101.53). All 6 carriers now reconcile (Healthspring penny-perfect, BCBS 27¢ rounding). **NEXT: AJ's UHC sign-off, then optionally M2 Phase 2 (content-page UI refinement) or the Medigap supplement parser.** KEY LESSONS: (a) validate against the raw-file completeness invariant + REAL agent names in the data (the Cyndi/Betty bugs passed unit tests but failed the real file); (b) customer-creating flows must be proven against real Postgres — partial-unique-index + autoflush + column-width bugs are invisible to SQLite tests.

**Where we are (2026-06-12, latest) — UHC + recap UX fixes batch (commits 604972c, a42bc91, c920d55, 4ac12ad, 3bfade2):** (1) **UHC HA payments → HRA** (own "HRA" recap group + member name parsed from action string; label "Bonuses"→"HRA" for UHC+Devoted). (2) Removed the manual-UHC entry form (Publish/Notify kept; double-count guard already suppressed legacy manual when a parsed UHC block exists). (3) **Customer names hyperlinked in the recap** — `persist_line_items` now back-links `CommissionLineItem.customer_id` by MBI (was 0% populated); backfilled 4,038 existing rows (Aetna/Healthspring/UHC ~complete; BCBS has no MBI so won't link). (4) **Reconciliation adjustments (migration 027, head now 027):** `CommissionAdjustment` per agent+carrier+period (signed amount + required note), admin form on the recap, flows into the carrier total as its own "Adjustments" group, visible to the agent. (5) **⚠ CRITICAL — UHC attribute by Writing Agent ID (col 4), NOT name:** Rebekah had $0 UHC because she writes her whole book under 'FOUNDERS INSURANCE AGENCY, LLC'; fixed via `AgentCarrierContract.id_value` map (`scripts/seed_uhc_writing_ids.py` seeded on VPS); after re-import **Rebekah is the TOP UHC earner $7,446.75 (927 lines)**, total still $70,466.86. (6) **Nightly Drive backup is DOWN** — rclone uses the shared default OAuth client_id which Google throttles globally (persistent `RATE_LIMIT_EXCEEDED`); retry flags added but the durable fix needs a private OAuth client_id in Tim's Google console (`scripts/RCLONE_OWN_CLIENT_ID.md`). Fresh on-box pg_dumps exist so data isn't at risk. (7) **#6 admin aggregate view BUILT + LIVE** — AJ picked **Option A (matrix)**. `/admin/commissions/aggregate` + "All Commissions" nav: agents × carriers grid, each cell = agent payout (matches their recap, incl. adjustments) over green Founders-keep, row/column/grand totals, month/YTD toggle + period picker, click-cell → that agent's recap. `build_aggregate_matrix()` in recap.py. Live-verified (Rebekah top earner $10,405 incl. her UHC $7,447; grand payout $43,176). (8) **$0.26 PARTD renewal → Founders override** (was quarantined as dust; per Tim it's a Part D plan override) — quarantine 89→48, total still $70,466.86. Migration head **027**. 230 tests.

**Where we are (2026-06-12, even later) — aggregate-view polish + carrier-status batch (commits through 134dde7, migration head now 028):** (a) **Matrix keep fixed + de-cluttered:** quarantine (needs_manual_review) \$ was wrongly counted as Founders keep — inflated Mike UHC to \$9,177; now tracked as 'pending' (NOT keep), surfaced as a '⚠ \$N pending review' badge; cells default to **payout-only** (clean) with a Material-3 'Founders keep' toggle revealing split-share/override on demand. (b) **Currency format** \$1,111.00 in the matrix; toolbar contained in one card. (c) **Cell click deep-links** the carrier drill-down: matrix cell → `admin_recap?...&carrier=X`, recap auto-opens that carrier's box (`?carrier=`). (d) **\$16 unattributed Humana fixed** — 'LEE JEFFREY C' writing agent 'RIDDLE BETTY B' now resolves to Betty via `_NAME_ALIASES` (needed a Humana re-import). (e) **#4 PERSISTENT CARRIER CONTAINERS + status (migration 028):** every agent recap shows ALL carriers they're contracted with, even at \$0; each tagged received / confirmed_zero / pending so '\$0 confirmed no business' ≠ 'statement not uploaded yet'. `CarrierPeriodConfirmation` model + `carrier_period_status()`; matrix carrier headers show a status dot or '⏳ confirm \$0' button (POST /admin/commissions/confirm-zero). Live: Rebekah's recap now shows GTL/Medico at \$0 'pending'. **Migration head 028. 232 tests.**

**Where we are (2026-06-13→16) — commission-admin UX overhaul (all shipped + live, head still 028, ~243 tests):** (1) **Unassigned-customer attribution bug FIXED** — unresolved commission rows used to default `primary_agent_id` to the UPLOADER (AJ) → 463 wrong stubs; root cause was `normalize_uhc` using the NAME col (Rebekah = 'FOUNDERS INSURANCE AGENCY, LLC') → now attributes by **Writing Agent ID** like the ledger; backfilled 460→Rebekah, deleted 1 junk DVH stub → **unassigned = 0**. Nameless+MBI-less rows (DVH Manual Payment) now `NON_CUSTOMER`. New **"Unassigned Customers" admin view** + nav badge + set-agent. (2) **Unified "Agent Commissions" module** — admin recap landing = the All-Commissions matrix (no agent_id → redirect), one nav item, agent-name links + back-link. (3) **Persistent responsive agent nav-bar** on matrix + recap. (4) **In-line quarantine resolver** — per-statement (`/admin/commissions/<id>/quarantine`) + **period-level all-carrier** (`/admin/commissions/review`, from the matrix's `N payments to review: $X` button): agent + override $ → `resolve_quarantine_line()` splits into agent_commission + founders_override, row leaves quarantine; carrier-agnostic w/ carrier column. (5) **⚠ HTTPS cert outage FIXED** — portal was down (expired cert; apt certbot crash-failing renewals due to pip cryptography vs apt pyOpenSSL); fixed via **snap certbot** (valid → Sep 14, auto-renew dry-run passes). **NEXT: build commission→customer ENRICHMENT + AOR timeline reconciliation** — spec `docs/superpowers/specs/2026-06-16-commission-enrichment-aor-reconcile-design.md` (Tim-answered §7). Phase 1 = AOR reconcile (newer enrollment end-dates older at prior month-end; dry-run backfill first; Tocara Brown = grounding case). Then fill-blanks enrichment (manual>BOB>commission), then PII via the provenance engine (agent-fix>commission, flag-once, new-address→flag-to-confirm), then surface payment dates on the profile. KEY: `PolicyPayment` ALREADY carries effective/term/statement_date/is_chargeback/commission_action — enrichment is mostly propagation. Also open in BACKLOG: disable old apt certbot/timer + stop VPS deploys leaking cryptography into system Python; M3 component-system spec awaiting Tim; off-site Drive backup still down (own OAuth client_id).

**Where we are (2026-06-11):** Security milestone substantially done — **S0 backups ✅, S1 ✅, S2 ✅** deployed+verified; **S3 ⛔ deferred** (trigger=white-labeling); S4 optional. In **Milestone 2 — UI/UX refinement**. **Phase 1 DEPLOYED 2026-06-11** (main + VPS, 198 tests green, no migration/deps; machine-verified live: login serves FOUNDERS title + grind/pestle + Plus Jakarta, dead Noto/Inter gone, founders-mark.svg 200, no errors — awaiting Tim's live browser eyeball of the animation + content pages light/dark): (1A) design SYSTEM in base.html — type scale (15px body/12px floor; fixed all 9-10px chrome incl. sidebar), radius 14/8/pill, spacing 4px grid, refined `.card`/`.badge`/`.btn`/`.data-table` (soft & tactile), global focus ring, fixed the invisible-text bug. (1B) **LOGIN fully rebuilt** to the locked design — frosted-glass card on an animated waving blue→green gradient, big "FOUNDERS / Agent Portal" title (white + navy glow, 0.10em), animated logo whose **pestle perpetually compounds** (Cannon Pharmacy origin); reduced-motion fallback verified; dead Noto/Inter fonts dropped (U3 win); reusable mark at `app/static/img/founders-mark.svg`. Headless-render verified (RM fallback shows full card; gradient pans blue→green). **Tim verified login 2026-06-11: "perfect for the login screen."** (The login logo is intentionally ALL-WHITE — by design, so it pops on the blue/green gradient instead of camouflaging; locked spec line 12. The blue&green `founders-mark.svg` is for in-app pages on light/white bgs — use it in Phase 2 sidebar/recap.) Tim also confirmed the content pages have hardcoded per-page styles still winning over the new tokens — EXPECTED (Phase 1 only touched base.html+login); that's exactly Phase 2's job. **NEXT: Phase 2 — refine each content page top-down (dashboard first), making them adopt the system tokens + drop their override CSS.** Phase-2 note: ~7 pages have `input:focus{outline:none}` that suppress the new focus ring — drop when refining each. Specs/plans: `docs/superpowers/specs|plans/2026-06-11-m2-phase1*` + `login-redesign-LOCKED-2026-06-10.md`.

**Where we are (2026-06-10):**
- ✅ Shipped & deployed: commission **R1** (ledger), **R1.1** (Devoted two-file), **R2** (agent recap + new Founders blue/green theme), multi-file **data-loss fix** (BCBS/Healthspring/Devoted), rounding fixes, **portal re-theme** (light+dark + toggle), **S0 off-site backups** (nightly→Google Drive, verified), **email migrated SendGrid→Brevo** (working).
- ✅ Shipped & deployed (2026-06-10): **S1 — Access hardening + session security** (on main, VPS deployed + live-verified). `app/security.py`: 12h session timeout (matches Google Workspace 12h reauth), hardened/capped cookies, security headers (HSTS/CSP/X-Frame/etc.), Flask-Limiter w/ per-agent two-tier key. New dep `flask-limiter`. Live-verified (headers/HSTS/429/login all confirmed).
- ✅ Shipped & deployed (2026-06-10): **S2 — Audit log + breach alerting** (on main, VPS deployed, migration 025 applied + live-verified). `app/audit.py` `log_event()` seam (logs login/data-view/export/admin/429 with IP/UA/agency/category/severity/record_count; defensive — never raises into caller), `app/alerts.py` 2 de-duped email triggers (non-domain/failed login + 429 flood; **exports are LOG-ONLY** — Tim works all hours so no off-hours rule), `/admin/audit-log` viewer, `docs/INCIDENT_RESPONSE_RUNBOOK.md`. VERIFIED live: migration 025 applied (head=025, 6 new cols + 2 indexes in Postgres), `log_event` writes/reads against real Postgres, `/admin/audit-log` 302→login unauthenticated (route+guard work), alert email path `send_email→True` via Brevo (Tim confirms inbox = final human check).
- ⛔ **S3 — Encryption-at-rest: DEFERRED 2026-06-10** (deliberate, with a trigger — NOT an oversight). Investigated: VPS disk is unencrypted (rented KVM, can't retrofit full-disk on a live box). App-level column encryption (MBI/DOB + searchable HMAC + migrate 5,000+ rows) is high-blast-radius + not necessary for 8 internal users (likely threats already covered by S1/S2/S0). RIGHT fix = migrate to an encrypted-volume/HIPAA host (infra, zero app risk). **Reactivation trigger = white-labeling** (encryption-at-rest then commercially required for a BAA; HIPAA host bundles encrypted disk). Full reasoning + preserved column-encryption design in [[s3-encryption-preview]].
- 🔜 NEXT: Tim's choice — **Milestone 2 (UI/IA refinement: text contrast + sidebar/module rework + mobile + login speed)** or **Milestone 3 (data quality: #6 name normalization + clickable names)**. S4 pentest (dep CVEs + `/security-review`) remains an optional worthwhile final pass on the S0/S1/S2 stack. Security milestone substantially complete (3 of the impactful pillars shipped; S3 deferred-by-decision, S4 optional).
- ⚙️ Working method (keep doing): brainstorm → spec (`docs/superpowers/specs/`) → writing-plans → subagent-driven build → VERIFY against rendered output/real data (not just green tests) → merge to main → deploy to VPS. Tests: `python3 -m pytest -q` (188 passing). Deploy: ssh root@23.187.248.100, git pull, `./venv/bin/pip install -r requirements.txt` (S1 adds flask-limiter), (flask db upgrade if migration), restart. Backup DB first if migration.
- 📌 Repo: `main` synced with origin. Migration head **031** (031 commission_line_items.manually_adjusted; 030 statement balance cols; 029 commission_line_item_revisions; 028 carrier_period_confirmations; 027 commission_adjustments — manual per-agent/carrier/period reconciliation lines; 026 widened payment_type 32→256; both applied on VPS 2026-06-12). Email = Brevo (raw `xkeysib-` key). DNS = nixihost.com (NOT cPanel). VPS DB backup: `PGPASSWORD=<from .env DATABASE_URL> pg_dump -U founders_user -h localhost founders_portal` (NOT plain `pg_dump` — that uses role 'root' which doesn't exist). VPS scripts need `PYTHONPATH=/var/www/founders-portal ./venv/bin/python3 scripts/x.py`.

<!-- ════════════════════════════════════════════════════════════════════ -->

Flask CRM/portal for a Medicare insurance agency. 8 agents. **Real AGENCY book (live prod, 2026-07): ~5,124 active policies / ~5,131 customers across 6 carriers (Humana 2,445, UHC 2,255, BCBS 237, Aetna 95, Healthspring 59, Devoted 33). Plan-type mix: MAPD 86.7%, PDP 6.0%, MA 4.4%, Medigap 0.7%, DVH 0.1%, Unknown 2.2%.** (The old "524 policies / 510 customers" figure in history entries below was Tim's SOLO book from the 2026-05 solo-agent era, before the full agency BOB + commission books were loaded — NOT the agency total.) All seeded/fake data deleted 2026-05-07.

## Stack
- Python 3.10, Flask 3.0, Flask-SQLAlchemy, Flask-Migrate (Alembic)
- **PostgreSQL 16** (Phase 2.5 complete — production database on VPS)
- Nginx + Gunicorn on Ubuntu VPS (23.187.248.100)
- Google OAuth 2.0 — restricted to @foundersinsuranceagency.com
- Vanilla JS only — no React/Vue. Jinja2 templates extending base.html.
- **Brevo** for transactional email (via `app/mailer.py`; migrated off SendGrid 2026-06-09), **Quo (formerly OpenPhone)** (primary VoIP) + **Retell AI** (missed call AI callbacks via Twilio SIP) + **Twilio** (SIP trunk for Retell AI + SMS blasts)

## Git Workflow
Local Crostini is the dev machine. Commit and push from local. VPS pulls.
```
git add <files> && git commit -m "message" && git push origin main
```
VPS deployment after pull:
```
cd /var/www/founders-portal && git pull && ./venv/bin/pip install -r requirements.txt && flask db upgrade && systemctl restart founders-portal
```
- VPS Python scripts: `./venv/bin/python3 scripts/myscript.py` (never plain `python3`)
- SSH: `ssh -i /home/timothywinslowlinux/.ssh/id_ed25519 root@23.187.248.100`

## BOB Parser Architecture (app/parsers/)

One file per carrier. Called via `parse_carrier_file(carrier, filepath)` from `app/parsers/__init__.py`.

**Detection** (`_detect_carrier()` in upload.py): scans up to 15 rows to find the real header row (carriers have preamble rows before headers). Checks first 2 bytes for `<` to detect HTML-disguised-as-XLS.

**Per-carrier format notes (portal BOB downloads, not commission files):**
- UHC: XLSX, 2-row preamble, header=2. Columns: mbiNumber, memberFirstName, memberLastName, memberAddress1, memberCity, memberZip, memberState, dateOfBirth, memberPhone, product, planName, memberCounty, policyEffectiveDate, policyTermDate (sentinel: 2300-01-01), agentId. Fingerprint: `mbinumber` + `memberfirstname`.
- Healthspring: TWO formats from same portal — (1) XLSX: 12-row preamble, header=12. (2) XLS: HTML-disguised, header=0, parse with read_html(). Both have identical columns: First Name, Last Name, Medicare Number, Member ID, Effective Date, Disenroll Effective Date, Date of Birth, Phone Number, Residential Address/City/State/Zip, Status, Product. Fingerprint: `medicare number` + `first name` + `disenroll effective date`.
- Humana: MBI not provided in BOB — matched by humana_id. `mbi` stored as empty string `""`. 196 Humana policies currently have mbi="" (not NULL) — do not add unique constraint on mbi without handling this first.
- Devoted: member_id is the Devoted member ID (not a UUID). Old seeded data used UUIDs — deleted 2026-05-07.

**Policy dedup logic (bulk_upload in upload.py):**
1. Match by carrier + member_id
2. Fallback: match by carrier + mbi (handles format changes where member_id differs between exports). On MBI match, adopt the new member_id as authoritative.

**One-time backfill scripts** live in `scripts/`. Run with `./venv/bin/python3 scripts/name.py` on VPS.

## Blueprint Registration Pattern
All blueprints registered in `app/__init__.py` with this exact 3-line pattern:
```python
from app.customers import customers_bp
app.register_blueprint(customers_bp)
```

## Current Blueprints
- `routes.py` — dashboard, admin overview, agent detail (no blueprint, registered directly)
- `auth.py` — Google OAuth
- `upload.py` — BOB import (agents + admins); commission statements (admin only via commission/)
- `labels.py` — birthday labels PDF
- `agent_settings.py` — carrier contracts, splits (admin-only)
- `commission/` — audit (admin + agent views)
- `customers.py` — customer list, profile, notes, contacts, merge (`customers_bp`)
- `pharmacies.py` — partner pharmacy CRUD + analytics (`pharmacies_bp`); admin-only add/edit
- `carriers.py` — plan database, plan detail, admin add/edit (`carriers_bp`)
- `comms/` — Quo/Twilio/Calendly webhooks, SMS, unmatched call resolution (`comms_bp`)

## Database Rules — READ BEFORE TOUCHING models.py OR upload.py

**Customer matching in _upsert_customer_from_policy() (upload.py):**
1. Match by MBI first (all carriers except Humana)
2. Humana: match by `humana_id`, then name+DOB+zip (ALL THREE must match)
3. If `customer.manually_edited == True`: do NOT overwrite phone, address, city, state, zip. Only update `carrier_address` and `last_carrier_sync`.
4. BCBS: `end_date` in CustomerAorHistory always set to None — BCBS term_date is a renewal date, not a termination.

**UHC/BCBS sentinel dates** (2300-01-01, 12/31/2199) must be treated as NULL.

**Betty Marlowe** has a 52.5% split (not 55%) — stored in `agent_carrier_contracts`.

**Flask-Migrate:** Every schema change requires a migration. Never use `db.create_all()` in production.

**Multi-tenant requirement (Phase 2.5+):** Every table gets `agency_id` FK (non-nullable). Every query MUST be scoped: `Customer.query.filter_by(agency_id=current_user.agency_id, ...)`. Missing agency_id = data leak across tenants.

**agency_id scoping sweep complete (Plan 03-07, 2026-04-03):** All Customer, Policy, CommissionStatement, CustomerNote, CustomerAorHistory, AgentCarrierContract, ImportBatch queries are now scoped. `_upsert_customer_from_policy()` takes explicit `agency_id` param — do NOT use `current_user` inside it. No new migration was needed — DB columns already existed from Phase 2.5; Plan 07 added ORM column definitions to models.py.

**BOB upload access:** `/upload` is open to all agents (not admin-only). Agent uploads attribute policies to `current_user.id` automatically. Admin uploads leave `agent_id` unset (matched later via carrier file). Agents see only their own import history; admins see all.

**commission_statements.agency_id:** Column was missing from DB — added manually via ALTER TABLE on 2026-04-13, migration 005 stamps this. If deploying fresh, `flask db upgrade` will apply it correctly.

**PostgreSQL sequence drift:** After bulk inserts or manual SQL, sequences can fall behind max(id). Fix with: `SELECT setval('tablename_id_seq', (SELECT MAX(id) FROM tablename));` — has affected `commission_statements` and `agent_carrier_contracts` in production.

## Plan Provenance Rules — READ BEFORE TOUCHING app/plan_provenance.py OR sync scripts

- **The seam:** ALL reads/writes of `_meta`/`_conflicts` go through `app/plan_provenance.py`. Nothing else touches the raw structure (keeps the future relational `plan_benefit_values` migration localized).
- **CMS sync writes via `set_cms_value()`**, never blind-merge. Precedence: empty→write; first-look (`unverified`)→CMS overwrites; `agent_entered`+match→promote to `human_verified`; `agent_entered`+differ→flag conflict (don't overwrite); `human_verified`→skip. Returns the action taken.
- **Year invariant:** logic operates on ONE Plan row `(carrier, cms_plan_id, year)`. Never compare across years — a 2027 first-look creates/updates the 2027 row, never touches 2026.
- **Benefit values are structured** (`make_value(amount, period, unit)`), NOT formatted strings — required for numeric filtering AND reliable conflict comparison.
- Human edits via `set_human_value(plan, field, value, user, note, verify=)`; `verify=True` = AJ marks `human_verified`. Also write an `AuditLog` row for agency-wide forensics (planned in editing UI = Plan 3).
- Tests run locally: `python3 -m pytest tests/test_plan_provenance.py` (SQLite in-memory, no VPS/venv needed).

## Agent Roster & RBAC (User.role, migration 019)

`User.role` ∈ `agent` (read-only shared data) | `senior_agent` (may edit) | `admin`. Gate via `can_edit_shared_data(user)` in `models.py` = `is_admin OR role in (senior_agent, admin)`. `is_admin` (from `ADMIN_EMAILS`, recomputed each OAuth login) **SUPERSEDES** role — single source of truth for admin.

- **admin:** Brian Freeman (owner+agent), AJ (admin@, builds carrier charts), Tim Winslow (developer+agent)
- **senior_agent:** Rebekah Long (principal, contracting), Justin Basinger, Chris Foster, Mike Lauzurique (LOA but trusted)
- **agent (read-only):** Anjana Patel, Betty Marlowe; new hires default here. **Alex** = not provisioned (not yet licensed; onboarding case study).
- Betty is also Brian's assistant (submits apps on his behalf) — delegation model TBD (see session notes).
- **Design principle for tech-averse agents (Betty/Brian/Rebekah):** simple default + few clearly-labeled controls, NO hidden modes/toggles. Behave like PioneerRx (their pharmacy software). Note: `tim@` is intentionally `is_admin=false` for admin-vs-agent view testing (his `role=admin` still grants edit rights).

## UX Design System — FOUNDERS RE-THEME (2026-06-09, SUPERSEDES the Lux theme below)

**The portal was re-themed to the Founders brand look** (the agent-recap design, applied portal-wide). Agents disliked the old Lux gold theme; this unifies everything on the look they approved.
- **Palette (tokens in `base.html :root`, single source of truth):** primary blue `#266EA5`, green `#65BB84` (positive/success, token `--green`), navy `#002E4D` (headings, `--ivory-bright`), text `#1A202C` (`--ivory`), muted `#718096` (`--slate`), page bg `#F7FAFC` (`--ink`), card `#FFFFFF` (`--surface`). DARK: page `#0E1726`, card `#172338`, blue `#4A9FD4`, green `#7BD49B`, text `#E2E8F0`, muted `#8AA0B8`, border `#24344D`.
- **Fonts:** Plus Jakarta Sans (body) + Merriweather (headings). `--radius:16px`, soft blue-tinted shadows (`--shadow`/`--shadow-sm`/`--shadow-float`).
- **Light + dark with device default + manual toggle:** `@media (prefers-color-scheme)` sets the default; `<html data-theme="light|dark">` (set by the sidebar-footer sun/moon toggle, persisted to `localStorage['fp-theme']`) overrides it. A no-flash pre-paint `<script>` in `<head>` applies the saved theme before first paint. No DB preference.
- **Semantic token ROLES are unchanged from the Lux scheme** (so the rules in "Color Token Reference" below still hold): `--ink`=background, `--ivory`=text, `--slate`=muted, `--gold`=accent (now blue), `--surface`=card. Only VALUES changed. `--gold` is now blue despite the name (kept for compat across 26 templates).
- **Recap** consumes the globals via `--rc-*: var(--…)` (follows light/dark). **`labels.html` still excluded** (print utility, hardcoded light colors). `login.html` is standalone (own `:root`) — re-themed to match.
- Spec/plan: `docs/superpowers/specs|plans/2026-06-09-portal-retheme*`. See [[r2-agent-recap]].

---

## UX Design System — OLD Lux/system theme (2026-05-04, SUPERSEDED by the Founders re-theme above)

**Decision: Replace dark mode entirely with system-aware light/dark theme.**
Use CSS `prefers-color-scheme` media query so the OS setting drives the palette automatically — no toggle needed, no DB preference stored.

### Light mode palette (default / `prefers-color-scheme: light`)
- Background: `#F5F4F2`, Surface: `#FFFFFF`, Surface-Low: `#EEEDEB`
- Text: `#1A1917` (ink), Secondary: `#6B6760` (slate)
- Gold accent: `#B8975A` (darker for contrast on light bg)
- Border: `rgba(26,25,23,0.10)`
- Status: error=`#C0392B`, progress=`#B8860B`, resolved=`#2E7D32`, waiting=`#5C4DB1`

### Dark mode palette (`prefers-color-scheme: dark`)
- Keep existing Lux palette: Ink `#0A0A09`, Surface `#131312`, Surface-Low `#1C1C1A`, Gold `#DAC495`, Ivory `#E5E2DF`
- Border: `rgba(76,70,61,0.18)`

### Shared design tokens
- **Fonts:** Noto Serif (headlines/metrics) + Inter (UI/body) — unchanged
- **Border-radius:** 6px (replacing 0px — softer, more approachable)
- **Padding:** cards get `20px 24px` (was `14px 16px`) — more breathing room
- **220px sidebar**, uppercase nav items — unchanged
- CSS vars defined in `base.html :root` with `@media (prefers-color-scheme: dark)` override block

### Implementation rules
- All color values must use CSS vars (`var(--bg)`, `var(--surface)`, etc.) — no hardcoded hex in templates
- `{% block styles %}` per-template CSS must also use vars only
- **`labels.html` exception:** Keep light-mode print colors hardcoded — do not use vars
- **Google button stays `#fff`** regardless of theme (Google brand guidelines)
- **`login.html`** — update to use vars; left panel uses `var(--surface)`, right uses `var(--bg)`
- Class names unchanged: `.card`, `.data-table`, `.btn-primary`, `.btn-secondary`, `.badge`, `.nav-item`
- Status badges keep muted jewel tone concept but adapt per palette

## Build Status
- **R4 — UHC commission parser SHIPPED LIVE + IMPORTED ✅ (2026-06-12)** — the 6th and last commission carrier wired through the normalized upload pipeline (recap + R1 ledger + customer sync + retired-agent rollup), matching the other 5. `normalize_uhc` + `_classify_uhc` in `app/commission/normalizers.py` (HA-bonus / pure-override $4.59 / sub-$1 PARTD dust → `NON_CUSTOMER` so they pay without spawning junk stubs); UHC registered in BOTH `NORMALIZERS` (normalizers.py) and `EXTRACTORS` (ledger.py); the stale "KNOWN-WRONG / not registered" warning removed. The R1 ledger extractor `extract_lineitems_uhc` auto-splits ~97.7%; the ~2.3% it can't (`NEEDS_MANUAL_REVIEW`: "New" enrollment proration cols L/T/AA/AB, PARTD dust, other-agent Med-Supp) are surfaced by `quarantined_line_items()` (recap.py — NO new column, queries the already-persisted rows) in a **Quarantine tab** (`/admin/commissions/<id>/quarantine` + a `⚠ N review` badge per statement in commission.html) + an upload flash, so nothing is silently dropped. **Retired-agent rollup (Cyndi/Don → Brian @0.50)** + **Betty's legal-name alias** ('RIDDLE, BETTY B'→Betty Marlowe via `_NAME_ALIASES` in routes.py) both apply on UHC. **LIVE on VPS:** UHC May 2026 imported (statement id=55, 3,944 line items, Σraw **$70,466.86** to the penny, 89 quarantined (2.3%), status=verified, Brian recap $4,765.85 incl. Don+Cyndi rollup, 0 duplicate-MBI customers). **TWO PRODUCTION HOTFIXES from the first real uploads (the offline rollback-dry-run missed both because it never flushed to Postgres):** (1) **migration 026** widens `commission_line_items.payment_type` 32→256 — quarantine rows store the full DVH Commission-Action string (up to 145 chars) and broke uploads with `StringDataRightTruncation`; (2) **commit c1ba8c2** wraps the resolver's 3 match-read helpers (`_crosswalk`/`_match_by_mbi`/`_find_name_dob_match`) in `db.session.no_autoflush` — a re-upload crashed with `UniqueViolation` on the partial index `ix_customers_mbi` because a match SELECT autoflushed a pending stub INSERT (a Postgres-only bug INVISIBLE to the SQLite test suite; `_create_stub` already flushes so later rows still match via SELECT). Re-upload proven idempotent on Postgres (customers_created=0). 221 tests. Commits ac0d5e3 (ship) + 7deb8a5 (026) + c1ba8c2 (autoflush). Spec/plan `docs/superpowers/specs/2026-06-11-uhc-raw-parser-design.md` + `docs/superpowers/plans/2026-06-11-ship-uhc-plan.md`. **REMAINING: AJ reviews a real upload run + the quarantine tab before UHC is system-of-record; the "New" enrollment math stays quarantined (irreducible).** See [[uhc-parser-resume]], [[retired-agent-rollup]], [[commission-balancing-system]].
- **S3 — Encryption-at-rest ⛔ DEFERRED (2026-06-10, deliberate decision — no code)** — Milestone 1 pillar 4. Investigated the VPS: disk is **NOT encrypted** (`/dev/sda1` plain ext4 on a rented KVM VM; full-disk encryption can't be retrofitted on a live rented box — it's an install-time/host operation). The only app-controllable at-rest protection would be **column encryption** (encrypt MBI/DOB in Postgres + a searchable HMAC of MBI so the BOB/commission matching pipeline still works + a migration encrypting 5,000+ live rows touching every MBI read/write). Tim's decision (agreed, real-talk): that's a high-blast-radius project (~S1+S2 combined, transforms the live column the whole matching pipeline depends on) defending only the *disk-bytes tail risk* (stolen disk image / rogue host / un-wiped decommissioned drive / leaked dump — someone with the raw bytes who never logged in); the *likely* threats (logging in) are already covered by S1 (OAuth+MFA+session) / S2 (audit+alert) / S0 (encrypted off-site backups). For 8 internal users, not worth it now. **RIGHT fix = infrastructure: migrate to an encrypted-volume / HIPAA-capable host (zero app risk, closes the gap directly).** **Reactivation trigger = white-labeling to other Medicare agencies** — then encryption-at-rest becomes commercially required (no compliance officer signs a BAA with unencrypted-at-rest PHI), and a HIPAA host with a BAA *bundles* encrypted volumes, so the host-migration path IS the white-label path. Column encryption then = defense-in-depth on top, not the requirement. Full reasoning + the preserved searchable-MBI HMAC design = [[s3-encryption-preview]]. No code, no migration this round.
- **S2 — Audit log + breach alerting ✅ DEPLOYED + VERIFIED (2026-06-10)** — Milestone 1 pillar 3. Turns the business-only `AuditLog` into a security audit trail + tightly-scoped alerting. **Two-stream design:** quiet complete DB log (never bugs you) + loud email alerts on only 2 outsider-flavored triggers. `app/audit.py` `log_event(action, *, category, detail, user, customer_id, severity, record_count, agency_id_override)` = the ONLY writer of AuditLog: captures real client IP (S1 ProxyFix `request.remote_addr`) + user_agent + agency_id + acting user; **defensive** (db commit wrapped in try/except+rollback → a logging failure NEVER raises into the caller's request; returns None on fail); folds customer_id into detail (PHI accountability — which record was viewed). `app/alerts.py` `maybe_alert()` = 2 rules only: (1) non-domain/failed login (`login_nondomain`/`login_failed`), (2) 429 flood (≥5 `rate_limit_blocked` from one source in 5min); de-duped (1 email per trigger+source per 5min via in-process throttle, empty-deque eviction prevents unbounded growth); plain-English email (What happened / Who-where / When / Access granted? / What to do) via existing Brevo `app/mailer.py` to MAIL_FROM. **Exports are LOG-ONLY (NO alert)** — Tim's call: agents work all hours (AJ at 3am, AEP till midnight+) so off-hours is a bad signal; exports logged with `record_count` for Tim's own review; volume-threshold alert deferred until a baseline is visible (one-line add later). Migration 025 adds ip_address/user_agent/agency_id/category/severity/record_count to audit_logs (all nullable; user_id stays nullable for failed-login rows). Hooks (~10 thin `log_event` calls): auth.py (login_success/nondomain/logout), security.py (`@app.errorhandler(429)`→rate_limit_blocked), customers.py (customer_view AFTER the 404 access-guard so blocked views don't log; customer_export_csv w/ record_count), labels.py (labels_pdf_download w/ count), agent_settings.py (agent_role_change), upload.py (migrated 2 hand-rolled AuditLog writes → log_event; commits batch BEFORE log_event). `/admin/audit-log` viewer (admin-only abort(403), **agency_id-scoped** = no cross-tenant leak, category/severity filters, 50/page, alert rows highlighted, no |safe = autoescape covers attacker-influenced detail; user_agent not rendered). Nav: admin "Audit Log". `docs/INCIDENT_RESPONSE_RUNBOOK.md` = Tim's action plan (Google Workspace suspend = kill switch since OAuth-only; `ufw deny` to block IP — ufw verified active; SECRET_KEY rotation = force all re-logins; Audit Log = extent tool; HIPAA flag; Tim primary + Brian for confirmed breach). No new dep. 13 tests in `tests/test_audit.py` (198 total). Code-review-hardened (defensive commit + customer_id fold + bounded state + autouse throttle-reset fixture). **DEPLOYED 2026-06-10** (DB backed up to `/root/founders_portal_pre_025_*.sql` → merge → VPS git pull → `flask db upgrade` 024→025 → restart). **LIVE-VERIFIED**: head=025, all 6 cols + 2 indexes present in Postgres, service active no errors, `/auth/login` 200, `/admin/audit-log` 302→login unauthenticated (route+guard work), `log_event` write+readback against real Postgres OK, and the alert path `send_email→True` via Brevo (a synthetic non-domain-login alert + a direct send both returned True; synthetic audit rows cleaned up, 29 real rows remain). CONFIRMED 2026-06-10: Tim received both test alert emails at admin@ — alerting verified end-to-end (logging + Brevo email both proven on live system). S2 fully done. Spec/plan `docs/superpowers/specs|plans/2026-06-10-s2-audit-log-breach-alerting*`. Builds on S1 (ProxyFix IP + 429 events). S3 (encryption-at-rest) next. See [[roadmap-2026-06-09]], [[s3-encryption-preview]].
- **S1 — Access hardening + session security ✅ DEPLOYED + VERIFIED (2026-06-10)** — Milestone 1 pillar 2. The portal is **Google-OAuth-only (no passwords)**, so S1 = session/header hardening, NOT password lockout (Google handles credential brute-force + MFA upstream). All in `app/security.py` `init_security(app)` (called once after blueprint registration in `app/__init__.py`): **(1) ProxyFix** (x_proto=1,x_for=1 — trust nginx scheme + real client IP; guarded against double-wrap). **(2) 12h absolute session timeout** matching Google Workspace's confirmed 12h reauth — `PERMANENT_SESSION_LIFETIME`+`REMEMBER_COOKIE_DURATION`=12h (config.py) + `session.permanent=True` added in `auth.py` callback() (REQUIRED or the lifetime is a no-op). Two layers (Google=identity/login; app=session-cookie lifetime) — NOT redundant; matching 12h = one clean daily login. **(3) Hardened cookies** Secure+HttpOnly+SameSite=Lax on session AND remember cookie (config.py). **(4) Security headers** (after_request): HSTS `max-age=31536000; includeSubDomains` **only over https** (so localhost dev isn't poisoned), X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin (stops MBI/ID leak via Referer), **permissive-but-real CSP** (keeps `'unsafe-inline'` so the no-flash theme script + onclick/onchange handlers + `{% block styles %}` keep working; locks origins + `frame-ancestors 'none'`; allows accounts.google.com for OAuth). Strict-CSP-with-nonces deferred. **(5) Flask-Limiter** (in-memory, new dep `flask-limiter`) with a **two-tier `key_func`**: authenticated → `user:<id>` (so up to 4 office-mates on ONE NAT IP get independent buckets — the office-NAT fix), anonymous → client IP. Limits: 600/hr default per agent, 10/min on `auth.google_login`+`auth.callback`, 60/min on `comms.{quo,calendly,healthsherpa}_webhook` (path convention `/comms/webhook/<name>` → future Twilio SMS inbound auto-covered). `_apply_limit()` writes the wrapped view back into `app.view_functions` (REQUIRED — discarding limiter.limit()'s return records but never enforces). `RATELIMIT_ENABLED` env (default on) disables in tests/dev. **Brevo email = outbound only, N/A.** Legit agent activity (SMS/email/app-submit/browse) is authenticated → per-agent path or outbound, NEVER the webhook limit. **No migration.** 8 tests in `tests/test_security.py` (188 total pass). Code-review VERIFIED live: get_remote_address returns real client IP behind ProxyFix, 600/hr backstop engages per-user, 429 genuinely fires. **DEPLOYED 2026-06-10** (VPS git pull → `./venv/bin/pip install -r requirements.txt` for flask-limiter → restart, NO db upgrade). **LIVE-VERIFIED**: `/auth/login` 200 over HTTPS, all 5 headers present incl. HSTS (proves nginx fwds X-Forwarded-Proto → ProxyFix → request.is_secure), rate limiter trips to 429 — gotcha: effective limit ≈2× stated (10/min → ~20/min) because `memory://` is per-worker and gunicorn runs `--workers 2`; nginx round-robins fresh connections across both so a small burst (<~14) won't trip, a bigger one (24x → 18×429) does — this is the documented/acceptable per-worker behavior, Redis is the upgrade path if exact limits ever needed. nginx already had X-Real-IP/X-Forwarded-For/-Proto. BROWSER-VERIFIED 2026-06-10: Tim confirmed login works for tim@ AND admin@, light + dark mode both render (CSP did not break theme script or Google OAuth). Fully verified end-to-end. Spec/plan `docs/superpowers/specs|plans/2026-06-10-s1-access-hardening*`. S2 (audit/alert) consumes these events next. See [[roadmap-2026-06-09]], [[s3-encryption-preview]].
- **S0 — Off-site automated backups ✅ DEPLOYED (2026-06-09)** — nightly encrypted (gpg AES256) DB+.env backup → Google Drive via rclone (`scripts/backup.sh`), 30-daily+12-monthly retention, one-command restore (`scripts/restore.sh`, defaults to `founders_portal_restore_test`, never prod without typing OVERWRITE PROD) + `docs/RESTORE_RUNBOOK.md`. Cron `/etc/cron.d/founders-backup` (nightly 3:15, weekly health Mon 8). rclone remote `gdrive:FoundersPortalBackups`. `BACKUP_PASSPHRASE` in VPS .env + Tim's Bitwarden (the recovery key — backup bundle contains .env, so off-box copy is the ONLY way to decrypt if VPS is destroyed). **VERIFIED**: full backup→Drive→restore round-trip; restored row counts matched prod exactly. First pillar of Milestone 1 (Security & Resilience). See [[roadmap-2026-06-09]]. Email alerts WORK (Brevo — see below).
- **Email: migrated SendGrid → Brevo ✅ DEPLOYED (2026-06-09)** — SendGrid's free tier lapsed ("Maximum credits exceeded") + email config was never actually loaded into Flask config (double-broken). Fixed both: ALL portal email now goes through ONE path `app/mailer.py` (Brevo transactional REST API via `requests`, attachment support); `app/labels.py` (birthday-label PDF) + `scripts/backup_report.py` (backup alerts, standalone urllib) both route through it. Config vars loaded in `config.py`: `BREVO_API_KEY`, `MAIL_FROM` (=admin@foundersinsuranceagency.com), `MAIL_FROM_NAME`. **Brevo key gotcha:** the REST API needs the RAW `xkeysib-…` key in the `api-key` header — NOT the base64 `{"api_key":...}` blob Brevo shows for its "MCP/SDK" config (that returns HTTP 401 "Key not found"). Domain authenticated in Brevo (DKIM CNAMEs `brevo1/brevo2._domainkey` added at NixiHost — DNS is at nixihost.com nameservers, NOT cPanel; existing Google-Workspace SPF/DMARC left untouched). Verified: account API 200, app send_email→True, backup health email sent. NOT for bulk customer blasts (future feature; needs deliverability + HIPAA/BAA). Old unused `SENDGRID_API_KEY` still in VPS .env (harmless, can remove). `requirements.txt` still lists `sendgrid` (unused, removable).
- **Phase 1 ✅** — BOB parsers (6 carriers), commission audit, agent dashboard, admin overview, birthday labels
- **Phase 2 ✅** — Customer master: Pharmacy, Customer, CustomerContact, CustomerNote, CustomerAorHistory models; customers_bp + pharmacies_bp blueprints; all 7 templates
- **Phase 2.5 ✅** — PostgreSQL 16 on VPS; Agency multi-tenant model; 2GB swap; Gunicorn gthread; 5,589 rows migrated; UAT passed 7/7; login page redesigned (dark glassmorphic, Inter font)
- **Phase 3 ✅ DEPLOYED (2026-04-13)** — Plans 01-07 complete and live on VPS. OAuth login fixed (https force + scope relaxation). Plan 06 still blocked on external provisioning (HealthSherpa + Google Meet Pub/Sub).
- **Commission Audit ✅ (2026-04-13)** — All 7 carriers now supported: UHC, Aetna, BCBS, Humana, Devoted, Healthspring, Wellable. Real March 2026 files uploaded and parsing correctly. See Commission Parser Notes below.
- **Commission override workflow ✅ (2026-04-13)** — Discrepancy → AJ submits explanation → agent accepts/disputes → AJ closes. stated_rate detection flags when AJ's formula rate contradicts contract rate.
- **BOB upload fixes ✅ (2026-04-29)** — Bulk upload now uses real form submit (flash messages work). Fixed agency_id/agent_id scoping in bulk_upload(). Fixed all PostgreSQL sequence drifts. Import history table: clickable rows open 3-tab detail modal (New / Updated / Not in this import = term report). Pending/error batches deletable with × button. _detect_carrier() now handles all 7 carriers as XLSX BOB files.
- **System-aware theme ✅ (2026-05-04)** — Replaced Lux dark-only theme with dual-palette system using `prefers-color-scheme`. Light mode default (#F5F4F2 bg, #FFFFFF surface, #B8975A gold); dark mode preserves Lux palette. All templates swept — hardcoded rgba() replaced with `color-mix(in srgb, var(--token) N%, transparent)`. Border-radius 6px, larger card padding, base font 14px. labels.html untouched (print utility).
- **Readability pass ✅ (2026-05-04)** — Font sizes bumped (9px→11px, 10px→12px, 11px→13px across all templates). All card-like containers (metric cards, panels, carrier cards, commission cards, drop zones) now have border + border-radius + real gap (12–16px, was 2px). Fixed duplicate Unmatched Calls nav item.
- **Dashboard fixes ✅ (2026-05-04)** — Removed duplicate period-banner (same data as metric cards). Termination items in timeline, tasks, and alerts panels now link to customer profile (MBI→customer_id resolved in route). NC Enrollment Windows → SEP Quick Reference with accurate May 2026 status.
- **Customers page enhancements ✅ (2026-05-04)** — Sortable columns (name, stage, pharmacy) via sort=/dir= URL params, server-side order_by. Column visibility picker dropdown (MBI, Phone, Agent, Stage, Pharmacy); prefs in localStorage. Agent column hidden by default for non-admins. Pager carries sort params.
- **Terminations redesign ✅ (2026-05-04)** — Rebuilt around priority (high/low/death) not urgency tiers. 30-day window only. New Policy columns: term_reason, new_carrier, new_plan_name (migration 007). Inline AJAX reason editor per row. Member names link to customer profile via MBI→customer_id lookup. Death rows show condolences nudge.
- **Pharmacy enhancements ✅ (2026-05-05)** — Fixed agency_id NOT NULL bug on pharmacy insert (500 error). Added pharmacy_agents many-to-many join table (migration 008). Pharmacy list: agent count chip on main row, 2-col expand (agents+customers left, policies by carrier+agent right as proper table). Agent location assignment moved to Agent Settings as multi-checkbox (migration 010+011 added then dropped primary_pharmacy_id in favour of pharmacy_agents). Pharmacy.agents has backref User.pharmacies.
- **Carriers & Plans database ✅ (2026-05-05)** — New Plan model (migration 009): per-plan-per-year records with CMS plan ID, plan letter (Medigap), lifecycle status (current/legacy/sunset/discontinued), self-referential successor chain, D-SNP/C-SNP/5-star flags, benefits snapshot, commission rates (initial/renewal/true-up/HRA bonus), BOB alias matching. carriers_bp registered. Plan list (filtered by year/carrier/type/status, carrier group headers), plan detail (chain visualization, member count, commission highlights, matched active policies), admin add/edit form. 34 plans seeded from BOB/commission data across 6 carriers. Humana chain 137→291→335 linked. Commission rates updated to real 2025/2026 CMS max broker rates (NC = "all other states"): MAPD 2026 $57.83/$28.92 PMPM, MAPD 2025 $52.17/$26.08, PDP 2026 $9.50/$4.75. Legacy/sunset plans (Humana 137, 291) left at placeholder rates — historical data to be added later.
- **Policy commission_type ✅ (2026-05-06)** — Migration 012 adds `commission_type` VARCHAR(16) nullable to policies. Values: NULL=unknown, 'initial'=first-ever MA enrollment, 'renewal'=all else. Customer profile: inline dropdown per policy row, saves via AJAX to POST /policy/set-commission-type. Commission audit: line items enriched via MBI→Policy lookup; Comm Type column shows Initial/Renewal badge in detail table.
- **Payment Ledger ✅ (2026-05-06)** — Migration 013: `policy_payments` table — one row per member per statement. `app/commission/payments.py`: per-carrier extractors for all 7 carriers + 3-tier match logic (exact MBI → carrier ID → fuzzy name). Populated on every commission statement upload. Routes: `/commissions/ledger` (agent) + `/admin/commissions/ledger` (admin with agent selector tabs). UI: period/carrier/type filters, summary cards (members paid, total, chargebacks, net, unmatched count), match confidence dots (green=MBI/ID, amber=name, red=unmatched), carrier group headers, chargeback rows in red.
- **Agent AOR Visibility ✅ (2026-05-06)** — Agents see only current AOR customers by default. Toggle to show former customers (read-only). Former-AOR banner on profile. All write operations (notes, contacts, SMS, pharmacy) blocked for non-current-AOR agents. BOB upload closes previous agent's open AOR history row on ownership transfer. `_customer_query(include_former)` + `_is_current_aor()` helpers in customers.py.
- **AOR history backfill ✅ (2026-05-06)** — `scripts/backfill_aor_end_dates.py` closes stale open AOR rows where stored agent ≠ customer.primary_agent_id. Found 0 stale rows on first run (data was clean).
- **BOB parser fixes ✅ (2026-05-07)** — UHC and Healthspring parsers rewritten for actual portal download formats. _detect_carrier() now scans up to 15 rows (was 1) and handles HTML-XLS files. Policy dedup falls back to MBI match when member_id differs between import formats.
- **Data cleanup ✅ (2026-05-07)** — Deleted 4,941 seeded policies and 26 Devoted UUID policies. Real dataset: 524 policies (all with MBI or humana_id), 535 customers.
- **Phase 4 — Data Integrity + Reconciliation ✅ DEPLOYED (2026-05-08)** — Migration 014: Humana mbi=''→NULL backfill (196 policies + 2 BCBS outliers), partial unique index `WHERE mbi IS NOT NULL` on customers.mbi, `unresolvable_json` column on import_batches. 25 shell customers (no MBI/humana_id/dependents) hard-deleted — 510 customers remain. MBI duplicate detection + side-by-side merge UI with single-transaction AOR-safe migration. Unresolvable BOB quarantine tab (4th tab in import modal with inline assign/MBI/create resolution). BOB↔Commission reconciliation page (members in BOB not paid + payments not in BOB, per carrier+period). Per-customer Payment History section on customer profile.
- **UI Polish ✅ (2026-05-07)** — Enhanced base.html with subtle CSS micro-animations, glassmorphism on sidebars (backdrop-filter), and depth effects via hover states for cards, buttons, and nav items without changing the core layout.
- **Commission parser fixes ✅ (2026-05-08)** — UHC and Aetna parsers corrected for April 2026 file formats. UHC: 29-col layout, agent=col5, mbi=col8, action=col19, commission=col23. Aetna: agency-level multi-agent file, agent_id=NULL on statement, per-row agent resolution via col16. PolicyPayment.agent_id made nullable (migration 015). Nickname matching (michael↔mike, christopher↔chris, etc.) added to both _detect_agent_id() and _resolve_agent_id(). paid=0 (no summary row) defaults to expected amount. extract_aetna and extract_uhc in payments.py updated to match new layouts with MBI now included.
- **Customer list phase 2 enhancements ✅ (2026-05-11)** — Carrier/plan_type/agent/medicaid filter bar (8 curated plan type options including "Medicare Advantage (All)" = `all_ma` value covering MAPD+MA+DSNP+CSNP). Stats strip (total/active/termed/medicaid counts). CSV export route `/customers/export`. Resizable columns (drag handles, drag-without-sort fix via `capture:true` one-time listener). Column picker expanded to 9 options: added Carrier, Plan Type, Plan Name, Eff. Year (all off by default). Saved views: personal localStorage + shared agency-wide via DB (`customer_saved_views` table, migration 016). Filter bar sizing fixed: `min-width:130px; max-width:200px; width:auto`.
- **Plan data single source of truth ✅ (2026-05-11)** — Migration 017: `Plan.friendly_name` VARCHAR(128) + `Policy.plan_id` FK → plans.id (SET NULL on delete, indexed). BOB upload now resolves `plan_id` via `_plan_alias_map(agency_id)` helper on every upsert. `scripts/seed_plan_aliases.py` populated all 38 plans with friendly names, BOB aliases, and resolved all 524 policy.plan_id values. Healthspring Cigna-named plans deleted; correct HealthSpring Preferred Savings HMO (H9725-015) created.
- **Plan names normalized + CMS IDs populated ✅ (2026-05-11)** — Redundant carrier prefix stripped from all 38 plan names (carrier is own table column). CMS IDs populated from `docs/All Insurance Plans for Zoho - Sheet1.csv` for all active plans. Key corrections: BCBS Freedom+ was H3894-009 → H3404-004; Aetna Eagle PPO was H5521-284 → H5521-241; Humana Choice PPO was H5525-034 → H5525-070. Humana PDP SOB data populated: Basic (S5884-133, $6.80/mo, $615 ded, 4-tier copays), Premier (S5884-154, $110.90/mo, $0 ded), Value (S5884-187, $32.00/mo, tiered ded). Service area = "Statewide NC" for all three Humana PDPs.
- **Carriers & Plans UI refresh ✅ (2026-05-11)** — Filter bar matches customers module exactly (same CSS classes, flex sizing, `onchange` submit). Carrier as dedicated column. Two-line plan name display: friendly name prominent, technical name muted below. Whole-row clickable; edit button uses `event.stopPropagation()`. SNP flags use `.flag-snp` (blue) vs `.flag` (gold). `plan_form.html` has friendly_name field above plan_name_aliases.
- **CMS sync scripts ✅ (2026-06-01)** — `scripts/sync_cms_plan_data.py`: reads CY2026 Landscape CSV, updates monthly_premium/annual_oopm/star_rating, deduplicates by plan across county rows, handles PPO "Not Applicable" consolidated premium fallback. `scripts/sync_pbp_benefit_data.py`: reads CMS PBP Benefits flat files (pbp_b4_emerg_urgent.txt + pbp_b7_health_prof.txt), updates pcp_copay/specialist_copay/er_copay. Both write reports to scripts/. Verified: UHC H5253-117 → pcp=$0, specialist=$35, er=$150, matches agent SOB. Source data: `docs/Medicare Landscape Files/` (not in git — scp to VPS before running pbp script).
- **Structured SOB plan benefits ✅ (2026-06-01)** — Migration 018 (drug_tier4/5, sob_url). `sync_pbp_extended_benefits.py` syncs 9 PBP files → details_json. plan_form/detail/list structured SOB sections. (Was listed as "NEXT" — now done.)
- **Plan Data Integrity & Provenance ✅ DEPLOYED (2026-06-03)** — `app/plan_provenance.py` is the seam owning structured benefit values `{amount,period,unit,display}` + per-field `_meta` (source/trust/history) + `_conflicts` inside `Plan.details_json`. `set_cms_value()` enforces CMS-vs-human precedence: first-looks yield to CMS, CMS never overwrites `human_verified`, CMS-vs-agent mismatch flags a conflict (the BCBS first-look-vs-CMS-approved incident preventer); CMS matching an agent value auto-promotes to verified. `resolve_conflict()` for AJ review. Migration 019: `plans.cms_synced_at`, `plans.has_unresolved_conflicts` (indexed), `users.role`. 55 tests in `tests/test_plan_provenance.py`. Spec/plan in `docs/superpowers/`. Consumers (sync retrofit + OTC/meals, editing UI, conflict queue, filter layer) = Plans 2–5, NOT yet built.
- **R1 — Commission Ledger Completeness ✅ (2026-06-08, on `feat/r1-commission-ledger`)** — `CommissionLineItem` (migration 023): a faithful 1:1 mirror of every amount-bearing commission-sheet row (full pre-split `raw_amount` + `classification` ∈ {agent_commission, founders_override, hra_bonus, chargeback} + carrier/agent/customer/payment_type, idempotent on `(statement_id, source_ref)`). `app/commission/ledger.py` = per-carrier extractors which, UNLIKE the customer-sync `normalizers.py`, do NOT collapse paired rows — the dropped Founders-override/Service-Fee row is exactly what makes balancing provable + `split_breakdown()` (derives agent_payout/founders_keep from raw_amount × split_rate; NEVER stored) + `verify_statement_balance()` (internal balance + independent re-sum completeness check that logs a loud warning naming carrier/period on a dropped row, but still imports — R1 is headless). Wired into `_ingest_normalized_upload` alongside the existing PolicyPayment writes (purely additive; PolicyPayment + normalizers untouched). 20 tests in `tests/test_commission_ledger.py` (all 5 carriers balance against real fixtures: Devoted sums to its own Total sheet 9824.99). UHC absent (R4). **Backfill = re-upload the 5 clean-carrier files via existing admin upload (no script).** Keystone for R2 (per-agent payout) / R3 (balance view). See [[commission-balancing-system]].
- **R1.1 — Devoted two-file support ✅ (2026-06-08, on `feat/devoted-two-file`)** — Devoted ships TWO structurally-different files/month: the agency book-of-business (`Total/Override/Agent Portion/HRA`, all agents except principal Rebekah) and Rebekah's per-agent statement (`Summary/Detail/Misc`, with a running balance). Both now parse into ONE monthly Devoted statement. `_devoted_format()` detects shape by sheet names; `_devoted_filetoken()` yields `agency` or `npn<NPN>`; every Devoted `source_ref` is `devoted::<filetoken>::<sheet>::<idx>` on BOTH `CommissionLineItem` and `PolicyPayment`; the replace-on-reupload delete in `_ingest_normalized_upload` is **file-scoped for Devoted** (`source_ref LIKE 'devoted::<token>::%'`) so the two files coexist and re-uploading one replaces only its rows (non-Devoted carriers keep blanket replace). Statement extractor: `Detail`→agent_commission/chargeback (same cols as Agent Portion), `Misc`→hra_bonus or **chargeback if negative** (HRA clawback); `Summary` NOT extracted (its Balance is a prior-period carryforward → would double-count; current-period only, e.g. Rebekah May = −$342.18). Negative agency Overrides reclassified `chargeback` with `split_rate=NULL` (Founders absorbs; rule: classification=label, split_rate drives math). Shared `_to_float` (both ledger.py + normalizers.py) now parses accounting `($N)` as negative. No migration (source_ref reused). 12 Devoted tests; synthetic `devoted_statement_sample.xlsx` fixture (built by `scripts/make_devoted_statement_fixture.py`). DEPLOY NOTE: before first post-R1.1 Devoted re-upload, one-time delete the pre-R1.1 un-tagged Devoted line items (`DELETE FROM commission_line_items WHERE carrier='Devoted' AND source_ref NOT LIKE 'devoted::%::%::%';`) or they linger as stale duplicates. **UPLOAD RUNBOOK:** the two Devoted files only coexist if both land in the SAME (carrier, period) statement — AJ MUST set the same 'Statement Month' on BOTH the agency file and Rebekah's statement file. The Rebekah file has no month in its filename, so without an explicit Statement Month it auto-detects and a soft warning fires reminding AJ to confirm the month matches. See [[devoted-dual-file-problem]].
- **R2 — Agent Commission Recap ✅ (2026-06-09, on `feat/agent-recap`)** — agent-facing single-screen recap built on the R1 ledger, with verifiability baked in (drill-down = the proof). `app/commission/recap.py` assembler: `build_recap(agent_id, agency_id, period)` → headline KPIs (total paid, new members gained/lost/net, net-after-chargebacks), per-carrier blocks (grouped New/Renewal/Chargeback, each block's line rows reconcile to Σ `split_breakdown`), lost members from `Policy` term data, % of book, YTD CY-vs-LY + run-rate (graceful "no prior-year data yet"). `is_new_enrollment(carrier, classification, payment_type)` per-carrier payment_type map (Devoted `initial - new/not new`, Humana `arcf/med2/...`, BCBS `fy/new`, Aetna `pro-rata payment`). founders_override + hra_bonus EXCLUDED (not agent commission). `AgentRecapPeriod` (migration 024) = publish workflow (draft→published, notify-once via `app/mailer.py`) + AJ-manual UHC figure (until R4, shown tagged "entered by AJ") + optional prior-year baseline. Routes: agent `/commissions/recap`, admin `/admin/commissions/recap` (+publish, +set-uhc), JSON `/commissions/recap/carrier` (lazy drill-down + member search). Template `commission/recap.html` = the new **Founders Material-3 look** (Plus Jakarta Sans 1.25rem + Merriweather headings; palette #266EA5/#65BB84/#002E4D from `docs/kadence-theme-export.dat`; 20px radius, soft shadows, big centered numbers, blue→green gradient New-Members card, brand-colored carrier cards, in-place Option-C drill-down: summary groups + member search + expandable rows + reconciliation footer "✓ lines add up to $X"). Nav: agent "My Commissions", admin "Agent Recaps". **This is the flagship of a NEW portal look** (agents disliked the old Lux theme); full portal re-theme deferred to a later project reusing these tokens. POLISH TODO: exact per-carrier brand hex (approximations now); VERIFY the New-Members gradient card renders in a real browser on deploy (CSS is correct — `.recap-page .kpi.gradient` linear-gradient with CSS-var colors, verified correct in isolation; only headless-screenshot rendering was inconclusive). 13 recap tests. UHC auto-calc = R4; agency balance view = R3. See [[commission-balancing-system]].
- **Commission multi-file data-loss fix ✅ (2026-06-09)** — per-agent/multi-file carriers were silently overwriting each other on upload (2nd agent's file wiped the 1st). Generalized the Devoted file-scoped-replace pattern: `PER_AGENT_CARRIERS` + `file_scoped_prefix(carrier, sheets)` in `ledger.py`. BCBS (one file PER AGENT, token = P-Number col A), Devoted (agency + Rebekah, token=npn), Healthspring (multiple NN_NNNNNN BATCH files, token = filename batch number via `current_upload_filename` ContextVar since no batch id is in the content). Agency-wide carriers (Humana/Aetna/UHC) keep blanket replace. Each file's source_ref = `carrier::token::...`; replace deletes only `carrier::thistoken::%`. Verified on real data (BCBS + Healthspring coexist). Also fixed: recap drill-down rounding (round payouts at line-item so rows reconcile to total exactly — was $431.01 vs $430.99) and split-rate display precision (52.5% not 53%). See [[healthspring-multi-file]].
- **Portal re-theme ✅ (2026-06-09, on `feat/portal-retheme`)** — whole portal re-skinned to the Founders look (blue/green, Plus Jakarta Sans + Merriweather, rounded/soft) in light + dark, via token-value swap in `base.html :root` (26 pages inherit). Device-default + sidebar sun/moon toggle (`data-theme`, localStorage `fp-theme`, no-flash pre-paint script). Recap follows globals; login re-themed (standalone); `labels.html` excluded. Verified both modes (dashboard/recap/login/customers). See the "UX Design System — FOUNDERS RE-THEME" section above. Features next: agent/month pills, upload checklist, agency totals view.

## Next Steps / To-Do

### CMS Data Sync — In Progress (2026-06-01)

Two complementary one-time scripts that populate plan benefit data from CMS bulk files. Both run on VPS. The `pbp-benefits-2026/` directory must be scp'd to VPS before running sync_pbp_benefit_data.py (too large for git).

- **`scripts/sync_cms_plan_data.py` ✅ committed** — reads `docs/Medicare Landscape Files/CY2026_Landscape_202603/CY2026_Landscape_202603.csv`. Updates `monthly_premium`, `annual_oopm`, `star_rating`. Writes `scripts/cms_sync_report.txt`.
- **`scripts/sync_pbp_benefit_data.py` ✅ committed** — reads `docs/Medicare Landscape Files/pbp-benefits-2026/pbp_b4_emerg_urgent.txt` + `pbp_b7_health_prof.txt`. Updates `pcp_copay` (b7a), `specialist_copay` (b7c = outpatient hospital specialist, confirmed $35 vs SOB), `er_copay` (b4a = ER with admission waiver, confirmed $150). Writes `scripts/pbp_sync_report.txt`. PDP plans (S5884-*) correctly land in "not found" — they have no b4/b7 rows.

**VPS run sequence:**
```bash
scp -ri ~/.ssh/id_ed25519 "docs/Medicare Landscape Files/pbp-benefits-2026/" root@23.187.248.100:"/var/www/founders-portal/docs/Medicare Landscape Files/"
ssh root@23.187.248.100
cd /var/www/founders-portal && git pull
./venv/bin/python3 scripts/sync_cms_plan_data.py
./venv/bin/python3 scripts/sync_pbp_benefit_data.py
```

### Plan Database — Full SOB Enhancement ✅ DONE (2026-06-01)

Migration 018 applied; structured SOB benefit fields in `details_json` (`make_value` shape via provenance layer); `plan_form`/`plan_detail`/`plan_list` updated with SOB sections. `sync_pbp_extended_benefits.py` populates 9 PBP files. See build-status entries. Remaining benefit-data completeness (OTC/meals from PBP b13, per-benefit nuance notes) = provenance **Plan 2** (sync retrofit + OTC/meals), not yet built.

### Phase 5 — Operations (after plan database)

Time logging, service tickets, lead source tracking, MedicareCenter PDF OCR, SOP hub.

Key items:
- Agent time log per customer interaction (minutes + activity type) from customer profile
- Service tickets (open/close/resolve) linked to customers
- Lead source field on customers for downstream analytics
- MedicareCenter enrollment PDF auto-parse into customer records
- SOP knowledge base (searchable)

### Future / backlog
- **Termination outcome tracker** — log of every termed policy since Jan 1, outcome (saved/converted/moved/deceased/fraud), contact history, ties to customer profile
- **AEP page** — dedicated AEP enrollment window tracker (separate from upcoming terminations)
- **Carriers & Plans** — customer profile → plan detail page link (match by carrier+plan_name)
- **Medicare.gov API** — annual plan refresh script for western NC zip codes during AEP prep (`data.cms.gov`, no auth required)

### Active Brainstorms (paused — resume, don't restart)
- **Commission→Customer Sync** — `docs/superpowers/Ideas/SESSION-NOTES-commission-customer-sync.md`. Bug: commission upload creates payments (CommissionStatement/PolicyPayment) but NOT Customer records. Paused awaiting AJ's RAW commission files (the ones in `docs/Commission DL/` are agent-cleaned, not source). Locked decisions: unified pipeline (row = payment + customer/AOR fact), stub customers (`stub=True, source='commission_import'`), dedup by stable ID not name, idempotent re-upload. Hard part: UHC LOA lumped-payment splitting (agent vs Founders share) — reuse the provenance pattern (inferred value + confidence + AJ phone-call override).
- **Customer Access Model** — interval-aware AOR access. Settled: full profile access on direct lookup (name/DOB/MBI); enumerate/report own+delegated book only (no fishing); Betty↔Brian delegation (explicit, modeless, attributed); OEP overlap via `CustomerAorHistory` intervals (current AOR active + future AOR in pipeline, date-driven). Queued AFTER commission sync (consumes the AOR intervals sync produces). Decisions in the session notes file.
- Triaged idea backlog: `docs/superpowers/Ideas/BACKLOG-triaged-2026-06-03.md`.

## Agent Nav — what's in the sidebar (as of 2026-05-08)
My Book: Dashboard, Customers, Duplicates (count badge, hidden when 0), Upcoming Terms
Commissions: Commission Audit, Payment Ledger, Reconciliation
Tools: Birthday Labels, Upload BOB Files, Carriers & Plans, SMS Templates
Alerts: Unmatched Calls

Admin nav additionally shows: Agency Overview, Agent Settings, Partner Pharmacies
**/forecast is NOT implemented** — do not add it to nav until the route exists.

## Phase 3.06 External Blockers (as of 2026-04-02)
- **HealthSherpa** — Agency admin account created, awaiting provisioning email from HealthSherpa. Use agency account (not individual agent). Once provisioned: register webhook URL + get HEALTHSHERPA_WEBHOOK_SECRET.
- **Google Meet Pub/Sub** — Tim is Google Workspace admin. Needs: Meet recording + transcription enabled for domain, Pub/Sub topic + subscription created, GOOGLE_APPLICATION_CREDENTIALS service account on VPS, GOOGLE_MEET_PUBSUB_SUBSCRIPTION in .env.
- Code for 3.06 can be written now; services just need to be registered once accounts are active.

## Phase 2.5 Pre-Code Checklist ✅ COMPLETE (2026-03-26)
- [x] Install PostgreSQL on VPS
- [x] Create `founders_portal` database + user
- [x] Update `config.py` DATABASE_URL
- [x] Run `flask db upgrade` — verify clean migration
- [x] Verify all data (commissions, policies, customers) present in PostgreSQL
- [x] Update `.env` with new DATABASE_URL
- [x] Add 2GB swap file to VPS
- [x] Update Gunicorn: `--workers 2 --threads 4 --worker-class gthread`
- [x] Remove SQLite from `requirements.txt`

## VPS Deployment Gotcha
- Always use `./venv/bin/pip install -r requirements.txt` on VPS — plain `pip install` installs to system Python, causing ModuleNotFoundError on startup
- Deploy command: `cd /var/www/founders-portal && git pull && ./venv/bin/pip install -r requirements.txt && flask db upgrade && systemctl restart founders-portal`

## VPS-Only State (not in git)
- `.env` on VPS has `SECRET_KEY`, `DATABASE_URL` (PostgreSQL), `ADMIN_EMAILS=admin@foundersinsuranceagency.com` — never commit
- `app/templates/base.html` on VPS had an extra `{% endif %}` (fixed 2026-03-26 during UAT) — local copy and VPS are now in sync
- Admin login: `admin@foundersinsuranceagency.com` (shared AJ+Tim). Agent test login: `tim@foundersinsuranceagency.com`
- `is_admin` is recalculated from `ADMIN_EMAILS` on every OAuth login — DB value gets overwritten
- `OAUTHLIB_RELAX_TOKEN_SCOPE=1` set in auth.py — required because Google Cloud OAuth app has Meet/Pub/Sub scopes configured, causing scope mismatch on basic login flow

## Phase 3 Pre-Code Checklist
- [x] Quo (OpenPhone) account provisioned — QUO_WEBHOOK_SIGNING_KEY + QUO_API_KEY in .env
- [x] Quo webhook URL registered: `https://portal.foundersinsuranceagency.com/comms/webhook/quo`
- [x] Retell AI configured with Twilio SIP trunking
- [x] Twilio account SID + auth token in .env
- [x] Calendly webhook active — CALENDLY_WEBHOOK_SECRET in .env
- [ ] HealthSherpa agency account — created, awaiting provisioning. Register webhook once active. Add HEALTHSHERPA_WEBHOOK_SECRET to .env.
- [ ] Google Meet: enable recording + transcription in Workspace admin, create Pub/Sub topic/subscription, add service account credentials to VPS, add GOOGLE_MEET_PUBSUB_SUBSCRIPTION to .env
- [ ] Distribute HealthSherpa captive join code to LOA agents once provisioned

## Commission Parser Notes (app/commission/routes.py + app/commission/payments.py)

Parsers are keyed by carrier name. Detection via `_detect_carrier()` fingerprints column headers. Agent matching via `_detect_agent_id()` (routes.py, gates upload) + `_resolve_agent_id()` (payments.py, per-row attribution).

**Column indices per carrier (verified against April 2026 files):**
- UHC: **29-col format** — agent=col5 (Writing Agent Name), mbi=col8 (MedicareID), action=col19, commission=col23, eff_date=col11, term_date=col28, term_reason=col24, stmt_date=col3, comp_type=col26 (I=initial/R=renewal-year), plan_type=col12, prior_plan=col27. No summary row in direct UHC downloads — paid defaults to expected.
- Aetna: **Agency-level multi-agent XLSX** — Payee=Founders (not an individual agent). agent_id=NULL on CommissionStatement. Per-row agent attribution via col16 (Writing Agent Name). mbi=col1, member=col4, action=col6, eff_date=col12, amount=col20. `AGENCY_LEVEL_CARRIERS = {"Aetna"}` in commission_upload(). Split rate read from any active Aetna contract.
- Humana: agent=col2, amount=col8 (PaidAmount). No separate paid row — `paid = gross`. **Every carrier (incl. Humana) pays Founders, never an agent directly** — there is NO direct-pay/split=1.0; Tim's Humana split is 0.55 like other carriers. Statements are agency-level (agent_id=NULL); per-agent earnings come from per-row PolicyPayment splits.
- BCBS: agent=col1, commission=col13. Summary row: `'$N x .55'` in col9, paid in col10.
- Devoted: agent=col2, amount=col11 (Base Amount). Summary row: `'N x .55'` in col8, paid in col9. Statement date is string `MM/DD/YYYY` in col0.
- Healthspring: agent=col3, amount=col7. Summary row: `'N x.55'` in col6, paid in col7. Also combines overrides + agent payments (same as UHC) — not yet handled.
- Wellable: agent=col3, advance_amount=col16. Summary row: `'$N x .55'` in col16, paid in col17. All line items flagged `is_advance=True` — clawback risk badge shown in UI.

**Agent name matching (both _detect_agent_id and _resolve_agent_id):**
- Files use "LAST, FIRST" or "LAST, FIRST MIDDLE" format; portal stores "First Last"
- `_normalize_name()` handles comma-separated format → "first last"
- Nickname dict covers michael↔mike, christopher↔chris, timothy↔tim, william↔bill, robert↔bob, richard↔rick, james↔jim, thomas↔tom
- Fuzzy fallback: same last name + first 3 chars match (catches Christopher→Chris)
- Falls back to None (agency-level) for unrecognised names (e.g. external LOA agents not in portal)

**paid=0 handling:** When `_scan_summary()` finds no summary row (carrier sends clean data files with no AJ annotation), `paid` returns 0. In `commission_upload()`, paid=0 is treated as "no summary present" and defaults to expected amount, marking statement verified. AJ corrects manually if actual payment differs.

**Split rates in agent_carrier_contracts:** (every carrier pays Founders, not agents — NO direct-pay/1.0 for anyone; statements are agency-level, agent_id=NULL)
- Tim Winslow: all carriers 0.55 (incl. Humana — the old "Humana 1.0 direct pay" was WRONG, corrected 2026-06-04)
- Mike Lauzurique: all carriers 0.525
- Betty Marlowe: 0.525 (stored; was incorrectly 0.55 in docs — actual is 52.5%)
- Aetna: 0.55 (55%) — AJ's March file used 0.525 by mistake; contract rate is 55%

**Known UHC behavior:** UHC sometimes pays gross×55% + separate HA bonus in a single disbursement. This shows as a discrepancy of the HA bonus amount — this is expected and should be reviewed, not auto-resolved.

**UHC override structure (LOA agents — PARTIALLY PLANNED, NOT YET IMPLEMENTED):**
UHC bundles agent commission + Founders override in one payment per member row. Applies to Mike Lauzurique, Rebekah Long, Betty Marlowe (LOA agents). NOT applicable to Aetna, Humana, BCBS, Devoted.
- Renewal rows: agent_base = Plan.comm_renewal PMPM ($347/12 = $28.9166). Override = total − agent_base. Net = agent_base × split_rate.
- New enrollment comp_type=R: agent_base = renewal_pmpm × months_remaining. Override = override_renewal_annual/12 × months_remaining ($55/yr for MAPD). months_remaining = 13 − effective_month.
- New enrollment comp_type=I ($517.50, $433.75): **PENDING AJ confirmation** — may be partial payment/true-up or plan-specific rate below CMS max. Do NOT implement split for comp_type=I until resolved.
- Implementation plan at `.claude/plans/we-need-to-discuss-buzzing-honey.md`.

**Wellable advance commissions:** 1st-year advances are clawback-eligible if policy lapses within advance period. Flagged with orange "Advance" badge and warning banner in commission detail view. Do not treat as verified income.

## Commission Statement → Customer/Policy Sync (PLANNED, NOT YET IMPLEMENTED)

On commission upload, the portal should enrich Customer/Policy records — not just store payment rows. Approved plan at `.claude/plans/we-need-to-discuss-buzzing-honey.md`.

**What gets updated on matched Policy (by MBI):** plan_type, plan_name (Contract+PBP formatted as "H5253-184"), effective_date (only if earlier), term_date+status='termed' (if present), term_reason, commission_type (I→'initial', R→'renewal'). Never overwrite PII (name, DOB, phone, address). Never overwrite if customer.manually_edited=True.

**Unmatched MBIs:** Auto-create stub Customer (stub=True, source='commission_import') + stub Policy. Agent fills in DOB/phone directly in portal to promote stub to full record — no BOB upload required. When BOB later includes the member, _upsert_customer_from_policy() fills remaining gaps.

**AOR discrepancies:** Commission file writing agent ≠ stored primary_agent_id → flag only, don't auto-update. Stored in CommissionStatement.aor_flags_json, surfaced as 5th tab in import modal.

**Pending migrations:** 018 — Customer.stub (Boolean), Customer.source (String), CommissionStatement.aor_flags_json (Text). (016 = customer_saved_views ✅, 017 = plan_link ✅)

## Key Files
- `FOUNDERS_PORTAL_CONTEXT.md` — full project context, agent roster, carrier details, roadmap
- `PRODUCT_VISION.md` — white-label SaaS vision
- `app/models.py` — all models
- `app/upload.py` — BOB import logic + `_upsert_customer_from_policy()`
- `.env` — secrets (not in git): GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SECRET_KEY, SENDGRID_API_KEY

## Color Token Reference (IMPORTANT — avoid light mode invisible text bugs)
In the dual-palette theme, token meanings flip between modes:
- `--ink` = **background** color (light grey in light, near-black in dark) — DO NOT use for text
- `--ivory` = **readable body text** (dark in light mode, light in dark mode) — use for text
- `--slate` = secondary/muted text (same role in both modes)
- `--gold` = accent color (darker in light, lighter in dark)
- `--surface` = card/panel background
- `--surface-low` = slightly recessed surface
- **Rule:** Any `color:` property for text must use `var(--ivory)` or `var(--slate)`, never `var(--ink)`.

## Session Protocol
At the end of every session (or after any push):
1. Update the **🧭 START HERE block at the top of this file** — current state + what's NEXT (this is what auto-loads into a fresh session, so it's the #1 thing that stops Tim having to re-explain).
2. **Reconcile `BACKLOG.md`** (repo root, the live backlog) — THIS IS THE ANTI-DRIFT STEP. Move shipped items to "Recently shipped"; add anything newly spotted; and **if this session broke or half-finished something, log it under "⚠ Regressions" so the side-effect is never lost.** A session that "fixed 8 of 10, broke 2" must leave those 2 + the breakage IN the backlog, not in Tim's head. Bump the `Last updated` date.
3. Update the latest `memory/session-handoff-*.md` (where we left off) and `memory/roadmap-*.md` if priorities changed; keep the ⭐ lines in `memory/MEMORY.md` accurate.
4. Update the relevant Build Status entry below + any spec Status.
5. Commit. Do not leave decisions undocumented.

**At the START of a session:** read the two ⭐ memory files (roadmap + latest session-handoff) named in the START HERE block, AND skim `BACKLOG.md` (esp. ⚠ Regressions + 🔴 items) before doing work — don't wait to "need" them.

**Working rule (anti-drift):** when Tim rattles off "do X, Y, Z" and some don't get finished, the unfinished ones go into `BACKLOG.md` before moving on — never silently dropped. Better to under-promise per session and keep the backlog honest than to leave a trail of half-done items.